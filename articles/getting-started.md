# Getting started

``` r

library(intraclass)
```

## Fit → ICC → interpret

This article will walk through the full pipeline on a clean, balanced
example: fit a variance-component model, obtain the appropriate
intraclass correlation coefficient with a confidence interval, and
interpret it.

> This is a placeholder. The worked example is written in **Milestone
> 1**, when the two-way random, absolute-agreement `ICC(A,1)` /
> `ICC(A,k)` estimator ships. See `project/MILESTONES.md`.
