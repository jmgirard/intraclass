# M150: The test suite's condition handling is exact

**Status:** done (2026-09-10, PR #168 https://github.com/jmgirard/intraclass/pull/168)

**Goal:** Every warning a test provokes is asserted at its site and the disposition grid's condition renderer never re-interpolates text a condition has already rendered, so `devtools::test()` reports `WARN 0` and a braced message cannot error the grid file.

**Outcome:** Tests only; nothing under `R/` changed and no NEWS entry owed. `render_condition()` in `test-n-o-disposition-grid.R` now strips ANSI and collapses whitespace over `conditionMessage()` alone, with no `cli::format_message()` call; a new test pins a literal `{x}` surviving, its abort passing `use_cli_format = FALSE` so a later rlang cli opt-in fails rather than errors it. The three warnings the suite leaked on main are asserted where they arise: the lavaan re-signal (`test-icc-lavaan-multilevel.R`) and the glmmTMB re-signal (`test-icc-type-vector.R`) by the engine's frame sentence with `fixed = TRUE`, wrapped around the existing `expect_error()`/`expect_message()`; the glmmTMB `raters = "fixed"` fit in `test-icc-brms.R` by class `intraclass_fixed_raters`. Suite at the review head: FAIL 0 / WARN 0 / SKIP 2 (vignette guards) / PASS 9471; `devtools::check()` 0/0/0; CI green on Ubuntu (R 4.5.0 and release) and Windows.

**Decisions:** none milestone-local. The plan gate chose asserting each warning by identity over `suppressWarnings()`, falsified by an assertion going red on an engine upgrade while the fixture still reaches the boundary.

**Review:** three-lens fan-out. [S] prior-review: no regression — the one prior finding on these files (M141 review [O] 1) is the brace defect this diff resolves; PR-comment probe empty. [S] blame-history: no conflict; frame sentences verified verbatim in the engine files. [O] diff-bug: six findings — fixed now: the brace test's coupling to the absent rlang cli opt-in (`use_cli_format = FALSE`), and the renderer comment naming `cli::format_message()`; rejected: frame-sentence assertions as platform-dependent (the plan-gate choice, CI on three platforms green), `expect_warning()` consuming one warning with a second escaping to the WARN line (that line is AC3's instrument), the brms site's pre-existing `skip_on_ci()`, and the test block's placement (style). Conversation: one Codecov bot comment, noted. Nothing graduated or retired at hygiene; the "Two test-suite defects" candidate row removed as shipped.
