# M123: Correct the falsified capability claims in the shipped documentation

**Status:** done (2026-08-17, PR #132 https://github.com/jmgirard/intraclass/pull/132)

**Goal:** Withdraw the four capability claims the shipped documentation makes that the package's own code falsifies, and pin each withdrawal so it cannot return.

**Outcome:** `README.Rmd`/`README.md` no longer call the Bayesian engine forthcoming (`brms` ships, with `ci_method = "posterior"`), name all four engines, and attribute the six non-base `Imports:` to what the package DECLARES rather than to what an install retrieves; `vignettes/multilevel-designs.Rmd` states inference-by-default and names `design =` as the disambiguator on incomplete data. Two transitive-install over-claims withdrawn from `NEWS.md` and the README. `cairn/DESIGN.md` and `CLAUDE.md` corrected in place — the same install falsehood, and an unbacked `augment` S3 method that never existed. Eleven spellings over six claims joined the one `claim_patterns` vector in `tests/testthat/test-doc-skew-caveat.R`; both walk legs gained `README.Rmd`/`README.md`, a per-line blockquote strip (a `> [!NOTE]` callout had left the marker mid-sentence after the join, defeating every pattern against the real sentence), and README non-emptiness floors. `data-raw/m123-capability-claim-mutations.R` commits the matrix: 88 source plants (11 spellings x 2 markup regimes x 4 wrap forms) and 33 installed plants against a real install, all RED against 0-failure controls.

**Decisions:** MD-1 (what the amended criteria establish and what they do not), MD-2 (supersedes MD-1's two AC6 paragraphs), MD-3 (corrects MD-2's exit mapping). None cross-cutting.

**Review:** Four attempts, three-lens fan-out each. Returns 1-3 (F1-F31) were floor returns: a pin that could not match its own blockquoted sentence; a new false install clause the correction introduced; a false `Rd:*` reachability rationale; an installed leg shadowed by `load_all`'s `system.file()` fallback; a NEWS bullet repeating the withdrawn framing; a vignette floor made vacuous under `R CMD check`. The thrash rule fired at return 3 on both triggers — third return, and AC6 failing twice by a new mechanism of the same shape — and the maintainer chose DESCOPE: AC5/AC6 narrowed to the demonstrated evidence, six pieces exiting to the "Per-class reachability proof for the doc-claim pin" candidate row as its (a) and (c)-(f), plus a new row for the README's dropped `lme4`-arrives-regardless disclosure. Attempt 4 met all eight criteria with no finding at the return floor: F32-F36 and F40 (stale counts, a wrong exit mapping, a ROADMAP over-claim) fixed before the gate, F37/F38 added to that candidate row as (g)/(h), F39 logged as plan-owned, F41 rejected. The `check()` differential against `main` and the mutation matrix were both re-run review-side.
