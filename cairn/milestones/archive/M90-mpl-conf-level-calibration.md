# M90: MPL κ_m recalibration + coverage GO/NO-GO at conf_level ∈ {0.90, 0.99}

**Status:** done (2026-07-24, PR #97 https://github.com/jmgirard/intraclass/pull/97)

**Goal:** GO/NO-GO on whether the two-way random ICC(A,1) MPL interval, with κ_m recalibrated at α∈{0.10, 0.01}, covers at nominal across the ρ range (incl. the near-zero boundary) at conf_level 0.90 and 0.99 — the prerequisite for M91.

**Outcome:** **Both levels GO**, so `ci_method = "mpl"` is cleared to export conf_level 0.90 and 0.99 (M91 wires it; M90 ships no `R/` code). Three seeded `data-raw/` scripts: `m90-mpl-kappa-tables.R` parametrizes the M86/M88 κ_m calibration by α and generates both tables (54 nodes each over R 2:10 × S {10,15,20,30,50,100}) → `m90-kappa-tables.rds`; `m90-mpl-coverage-sweep.R` sweeps 8 decisive cells × 2 levels recording per-cell miss-side/width/clamp diagnostics → `m90-coverage-sweep.rds`; `m90-mpl-verdict.R` applies the frozen floors → `m90-verdict.rds`. **BC1**: the α-parametrized pipeline reproduces xiao2013's *published* α=0.10 κ_m 6/6 within ±0.10 (max |diff| 0.020), incl. the off-`s_grid` S=25 geometries — conf_level 0.90 therefore has a direct external oracle, unlike the shipped 0.95. **BC2**: α=0.01 κ̂_m bootstrap SE 0.037/0.033/0.026 for R∈{2,3,10}, all ≤0.05. **Coverage**: every cell clears its floor (0.90 min 0.915 at C4 ≥ 0.88; 0.99 min 0.997 at C6 ≥ 0.98), including the new C8 sub-grid-floor cell (ρ_true=0.02, below the κ_m grid's ρ_L=0.05 fence). Costs recorded for M91's docs: misses are near-entirely one-sided at the δ=4 S↑ cells (C4/C6 84/1 and 75/6 at 0.90 — the two-sided interval is far from equal-tailed), and 0.99 runs wide (max median width 0.852, below BC6's 0.90 doc trigger). Evidence: `references/mpl-twoway-random-comparison.md` (§ M90 pre-registration, frozen before any run per GP5; § M90 verdict).

**Decisions:** D-017 (RB03/RR03 Fable review: 0.90 GO on the published oracle; 0.99 conditional GO on simulated coverage alone, gated on BC1–BC7 — ingested verbatim as AC4–AC10; rejected a tail-model κ_corr estimator and a categorical hold on 0.99; no level deeper than 0.99 authorized).

**Review:** 3-lens fan-out + scorer — no correctness defects (diff-bug [O] verified all three scripts and every recorded number against the fixtures; prior-review [S] zero regressions; blame-history [S] one finding). Actioned: **F1 (84)** — the note's Provenance line named only the pre-registration, omitting the verdict M90 also appended (M72 stale-summary pattern) → corrected. Logged sub-threshold: **F2 (34)** result-neutral cross-cell RNG-state overlap in a research script. AC1 was sent back once mid-review and gate-amended (it mis-described the scan→top-k bias correction as a consistency check). All 10 AC verified fresh; every RR03 projection cleared with no shortfall; PR CI green 9/9.
