# M88: Exported profile-likelihood `ci_method = "mpl"` — two-way random ICC(A,1)/ICC(A,k)

**Status:** done (2026-07-24, PR #95 https://github.com/jmgirard/intraclass/pull/95)

**Goal:** Export the M86/M87-validated modified-profile-likelihood interval as an opt-in `ci_method = "mpl"` for the balanced-complete two-way random ICC(A,1) (and ICC(A,k) by Spearman-Brown inheritance), per the D-014 GO-for-opt-in.

**Outcome:** New `ci_method = "mpl"` (`R/ci-mpl.R`): the deterministic MPL interval (Xiao & Liu 2013) for two-way random ICC(A,1), with ICC(A,k) the `npb_sb()` Spearman-Brown image. κ_m ships as a precomputed 54-node (R=2:10 × S∈{10..100}) table in `R/sysdata.rda`, generated by `data-raw/m88-mpl-kappa-table.R` (M87 cross-check ALL PASS). Fenced to balanced-complete two-way random absolute-agreement / Gaussian / `conf_level = 0.95` / one rating per cell; all else aborts loudly (#5). Deterministic metadata (samples/std.error NA, engine REML point) mirrors searle/burch; print labels "modified profile likelihood". Oracle O-MPL registered; xiao2013 is now shipped code. NEWS entry added.

**Decisions:** D-015 (exported-API scope: precomputed κ_m table, ICC(A,k) via SB inheritance, name "mpl", the estimand fence). AC1 and AC3 amended at gates (deterministic Example-1 in-suite oracle; the SB-inheritance identity is inheritance-not-anchor, not a "direct McGraw-Wong" construction).

**Review:** 3-lens + scorer. 2 findings actioned with regression tests: the fence now aborts within-cell-replicated designs (96; was returning a mis-calibrated interval) and the consistency-drop emits a `cli_inform` (87; ADR-054 convention). 1 below-bar (74, inverted κ_m-interpolation comment) corrected as known-false. Prior-review clean.
