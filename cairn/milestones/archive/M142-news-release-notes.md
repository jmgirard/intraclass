# M142: NEWS.md's 0.1.0 entry reads as first-release notes, not a development log

**Status:** done (2026-08-27, PR #153 https://github.com/jmgirard/intraclass/pull/153)

**Goal:** Rewrite the `# intraclass 0.1.0` entry of `NEWS.md` as first-release notes — what the package does for someone meeting it — instead of the 36,201-byte development narrative it was.

**Outcome:** The entry is 5,262 bytes: a launch overview plus *What ships*, *Engines*, *Confidence intervals* and *When a call fails*, with the three guard-pinned regions carried through — the `width_templates()` run, the `residual_template()` run, and the `news_scope()` anchor bullet whose three `mpl-doc-claims.tsv` rows quote it (one re-keyed `87f0dfc36b75` → `0be2190a0d90`). `cairn/doctrine/prose-style.md` and `cairn/DESIGN.md`'s pointer to it now name `NEWS.md` as a governed surface. New instrument `data-raw/m142-bullet-lines.awk`: per-bullet BYTE sizes, since a line cap is defeated by reflow alone; its header records that the `-`/`+` widening can SPLIT a bullet, so the a-fortiori reading of AC1's 500-byte cap holds on this file (neither revision uses those markers) and not in general. Final shape: 1 heading, 15 bullets, one over 500 bytes and it is the named 797-byte anchor exemption; 46 sentences, 0 dashes, one sentence over 35 words and it carries the 58-word pinned residual clause.

**Decisions:** none promoted. Milestone-local: the plan gate chose condensation by deletion and cross-reference over composed summaries; the mechanical invented-or-widened-claim check was descoped at the user's decision after three audit passes, absorbed as (c) of the "Three prose-apparatus deferrals" candidate row; AC1 took one substantive amendment, the exempt anchor bullet's figure 1,008 → 808 measured rather than recited.

**Review:** Two rounds, three-lens fan-out each (user-facing tier). Round 1 returned the milestone: no criterion failed, but the read-through found the class the descoped check was left to catch — condensation had dropped bounding qualifiers, four resulting sentences asserting capabilities `icc()` refuses with classed aborts, plus an invented "Seven articles" count. Defect-return count: 1. The repair round, at the user's direction, deleted the capability bullets rather than restoring their bounds. Round 2: all five criteria pass on fresh evidence, gate clean, 22 findings. Fixed at the merge gate: a false install claim (a plain install DOES fetch `lme4`, via `glmmTMB`'s Imports — it contradicted `README.md`), a `summary()` method asserted for both classes when only `summary.icc` exists, a truncated article title, an unbounded per-method universal, the dropped `ggplot2` Suggests fact, the `DESIGN.md` pointer, the awk header's false a-fortiori claim, and two work-log records ("byte-identical" regions that were reflowed; a 15-bullet figure that was 14). Filed as one candidate row, "What the 0.1.0 NEWS entry no longer says": the surviving simulation bullets' missing antecedents, the absence of multilevel/incomplete-data support from *What ships*, D-019's now-stale "documented in NEWS", and the vanished brms guarantees. Rejected at triage: seven, all pre-existing or already dispositioned.
