# M137: R6 claim audit over the M134-M136 prose diffs

**Status:** done (2026-08-25, PR #146 https://github.com/jmgirard/intraclass/pull/146)

**Goal:** Read every chunk of user-facing prose that M134, M135 and M136 rewrote against the text it replaced, and repair each claim whose scope moved.

**Outcome:** All 111 hunks the three passes changed on user-facing paths (`bb42f8e` 32, `8aafb0e` 52, `5c274fc` 27) compared against the text each replaced; the per-hunk triage table with its verdicts is in the milestone file (git). 110 `same`, 1 `wider`: `vignettes/interval-methods.Rmd`'s `"mpl"` sentence had come to promise the method "does not abort at the near-zero-ICC boundary", where the replaced text bound the promise to the cells where the two-way Monte-Carlo default aborts — D-019's two abort paths (degenerate fit, crossing-indicated root-finding failure) survive there. Repaired by restoring the bound in new wording, which drops `data-raw/mpl-doc-claims.tsv` row `0e83d1125bf2` (its quote was the widened clause; `check-mpl-doc-claims.py` 60 candidates, 0 failures). Five further claims repaired on their own merits under the Scope's cheap-in-place clause: `?icc`'s `@param level` conflated gloss ("agreement-only, complete crossed designs" — falsified by `R/estimand.R:87-102`, `R/icc.R:1179-1200` and the incomplete-conflated path at `R/icc.R:1487`; M17/ADR-026's restriction, superseded by ADR-056 and ADR-028); `vignettes/engines.Rmd`'s two-level SEM route ("complete, balanced data with equal cluster sizes" is the *fixed*-rater path, not the random-rater one); `vignettes/interval-methods.Rmd`'s unqualified "names an alternative method" (`R/icc.R:371`: where none serves, it names none); the NEWS bullet's "against the text they replaced" framing; and an orphan roxygen wrap. `cairn/doctrine/prose-style.md` step 5 rewritten to select every hunk and name the scope-carrying construction class (quantifier/absolute, restrictive clause, dash- or parenthesis-set appositive, frame adverbial, conditional, hedge) instead of ten scope words — the net M136's three closures each escaped; 118 lines / 6,941 bytes against its 120 / 8,000 budget. Nothing the package computes or reports changed.

**Decisions:** one milestone-local (T5): the `"mpl"` sentence is repaired by restoring the replaced text's scope in new wording rather than reverting, the pre-M135 sentence having carried a semicolon splice the pass removed; the ledger row is removed rather than re-keyed because the restored wording enumerates as no candidate.

**Review:** full three-lens fan-out (user-facing tier). Prior-review lens: no findings — the repo has no inline PR review comments at all, and the archived record shows the diff applying the M130 ledger lesson and M136's word-list lesson. Blame-history lens: no findings; it independently reached the `wider` verdict and traced the `level` wording's supersession. Diff-bug lens: ten ranked findings — six actioned and fixed on the branch before the marker (NEWS framing, the `engines.Rmd` claim, the doctrine's own R6 paragraph widening under compression, a triage note attached to the wrong hunk row, the unqualified abort-hint claim, the roxygen wrap), four rejected with reason (the `—`→`no` matrix cells as a defensible verdict, the `@param level` gloss as a pointer matching the Details section, the ledger drop, row 64's note). No finding failed a criterion inside its procedure's domain; no send-back. Nothing retired or graduated.
