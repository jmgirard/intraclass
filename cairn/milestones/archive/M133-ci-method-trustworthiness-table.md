# M133: Tell users which interval method is trustworthy for their design

**Status:** done (2026-08-23, PR #142 https://github.com/jmgirard/intraclass/pull/142)

**Goal:** Pin, in the test suite, at least one call each `ci_method` computes an interval for and at least one call it refuses.

**Outcome:** Four blocks in `tests/testthat/test-vignette-claims.R` pin the seven `ci_method` fences: a supported call per frequentist value asserting a non-`NA` interval bracketing the estimate plus the coefficient family and `method` label; `posterior`'s behind a live brms fit gated `skip_on_ci()`; a refused call per value pinned to its fence message and `intraclass_unsupported`; and second supported calls for `npbootstrap`'s and `mpl`'s numeric-`unit` projections. Membership is enforced by scraping `validate_choice()`'s own abort message, never a hand-list. `vc_mpl_sim()` now shares the article's seeded 20x4 simulation with the older `"mpl"` fences test. `ORACLES.md`'s O-MPL Decision line corrected in place (the M91/D-017 `conf_level` set) and its M74 triage row rekeyed. The seven-row vignette trustworthiness table this milestone set out to ship was REVERTED — `vignettes/` and `NEWS.md` are byte-identical to main.

**Decisions:** none promoted. Milestone-local: the table's cells were composed from a reading of the fences rather than derived from code, and two review rounds each found new cells false — the descope narrowed the promise to the pins and sent the table to its own ROADMAP candidate row (D-029: user docs are not barred; only the standing guard needs D-021 superseded first).

**Review:** three rounds. Rounds 1 and 2 were defect returns on the table (AC1 and AC3, five then three false cells); the user chose descope over a third retry. Round 3 on the narrowed set: all three amended criteria pass on fresh evidence (21 plants red, both controls green; suite 0 failures / 8599 passing; both `R CMD check` tarballs raw `Status: OK`), gate clean, full three-lens fan-out. Eight findings — five fixed on the branch (an `ORACLES.md` "superseded" wording contradicting D-017's own Consequences, plus its ledger rekey; a duplicated `montecarlo` assertion with a false comment; the block header's lavaan fence attribution; an overclaiming comment; an M127 measurement dropped by a ROADMAP compression), one follow-up, two rejected. Follow-up carried here: the `posterior` half is invisible to both anti-vacuity enumerators (`setdiff(..., "posterior")` plus `skip_on_ci()`), joining the pin-coverage gaps on the table's candidate row. LESSONS: no line added, nothing retired — the one durable lesson (an in-place `cairn/references/` correction orphans its M74 triage row) went to `cairn/doctrine/data-raw-checkers.md`, whose slot owns it.
