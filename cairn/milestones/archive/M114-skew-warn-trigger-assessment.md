# M114: Runtime skew/kurtosis warn trigger — design & validation (assessment only)

**Status:** done (2026-08-08, PR #123 https://github.com/jmgirard/intraclass/pull/123)

**Goal:** Decide, against pre-frozen reliability criteria read over per-rep trigger statistics joined to the committed M111 coverage data, whether a data-measurable runtime statistic can reliably signal the one-way MC default's skew/kurtosis under-coverage (D-027's `warn` commission).

**Outcome:** No code. Frozen page `cairn/references/mc-skew-warn-trigger.md` (48-candidate closed family: shipped `burch_kappa_hat`/`burch_kappa_bc` κ̂_bc + skewness γ̂ on the same z-decomposition; tiered floors W1 ≥0.90/W2 ≥0.50/W3 ≤0.10; mechanical selection ordering) committed before any derivation artifact. `data-raw/m114-warn-trigger-derivation.R` seed-regenerated all 64×2000 M111 reps (identity proof: 128000/128000 SEARLE endpoints within 1e-12), shared helper `m114-trigger-stats.R`, held-out sweep `m114-heldout-sweep.R` (ids 65–74: lognormal/laplace/gaussian, off-grid (20,3); every (50,5) lognormal/laplace cell under-covers, worst 0.825), verdict `m114-warn-trigger-verdict.R` + 138k-row stats table + ledgers, all byte-stable. **Verdict D-028: DEGRADE to `document`** — 0/48 pass W1 or W2 (best min targeted fire 0.1625); cause is dilution (cluster effect at weight² ≈ 0.04 in the pooled z at n = 5), so reopening routes to a cluster-effect-direct family, never re-thresholding. Candidate rows: document-caveat response milestone; direct-family reopening.

**Decisions:** D-028 (cross-cutting). Milestone-local: implement gate fixed the tiered floors and made the 26 high-abort cells descriptive-only (D-026's phenomenon, not this trigger's target).

**Review:** One amendment return (AC1's freeze-edit clause quantified over the whole page, forbidding the scheduled T5 results fill; scoped to frozen-rules content, diff-verified untouched). Fan-out 7+1+1 findings; scorer actioned 0 (≥80); 9 logged sub-80, F1 (74, page/D-028 paired cell 56's coverage with cell 60's median) fixed pre-merge. Diff-bug lens independently reproduced classification, metrics, and verdict. CI: two reference-gate fixes (D-009 directives + 10 triage rows for the new page); pre-existing stale roadmap-terminal-rows record-claim expectation (M112 rotation) fixed in passing. LESSONS: M113 freeze-edit line scoped to frozen-rules content; freeze-fence-criterion + record-claims-rotation lines added; graduated the M65/M71 absence-claim lines (owned by tracking-rules' dated-observation doctrine, enforced by the D-009 checker that redded this PR's CI).
