# M126: Disclose what an installation actually retrieves

**Status:** done (2026-08-18, PR #135 https://github.com/jmgirard/intraclass/pull/135)

**Goal:** Restore, across the three shipped surfaces that discuss dependencies, the disclosure that `lme4` arrives with any installation while `merDeriv` does not.

**Outcome:** `README.Rmd`/`README.md`, `NEWS.md`'s unreleased `0.1.0` clause plus a development-version bullet, and `vignettes/engines.Rmd`'s mixed-model section now state that `glmmTMB`'s own `Imports:` puts `lme4` on the library path whatever its `Suggests:` placement here implies; that `engine = "lme4"` additionally needs `merDeriv`, checked by `rlang::check_installed()` at the entry of all 12 `fit_lme4*()` fitters before any `ci_method` branching, so `glmmTMB` is the only engine a plain install leaves usable; and that asking for `merDeriv` pulls `lavaan` through merDeriv's own `Depends:`. No surface states a count of retrieved packages — the two obvious procedures disagreed at every measurement (63/60 at T1, 71 at review 1, 72/61 at review 3) while agreeing on membership. `claim_patterns` in `tests/testthat/test-doc-skew-caveat.R` gained five withdrawn-wording spellings (`install_not_required_{marked,bare}`, `install_light_{marked,bare}`, `install_one_required_dep`), each anchored to the contiguous clause it shipped in — `install_light_*` to its full stop — and mutation-verified RED 128/128 on the source leg and 48/48 on the installed leg by `data-raw/m123-capability-claim-mutations.R`, controls clean.

**Decisions:** milestone-local — membership claims (`lme4` arrives, `merDeriv` does not) are shippable on a user-facing surface where a cardinality claim is not: each membership claim is set by one declaration in one file (glmmTMB's `Imports:`, this package's `Suggests:`), while a count is the size of a transitive closure that changes with no event this repo could notice.

**Review:** three attempts, two defect returns — return 1 for prose asserting the lme4 *engine* was usable on a stock install when only the *package* arrives; return 2 for a `weight caps` gate failure (plan-owned body 154 against <150) plus AC7 failing as literally written. Attempt 3: gate green (`cairn_validate` 16 PASS, `check()` 0/0/0, installed suite 8813 passing at 0 skips), both [S] lenses zero findings, [O] diff lens 11 findings — six repaired at the gate, chiefly a branch-added "none of them is fetched unless you ask for it" that merDeriv's `Depends:` falsifies; five rejected or non-findings. Deferred to a candidate row: the 12 `check_installed("merDeriv")` `reason` strings still framing the requirement as interval-method-specific. Post-merge: two lessons added, the two stalest (M98, M99) pruned to hold the LESSONS byte budget.
