# M117: State the `"burch"`/`"searle"` width relationship conditionally

**Status:** done (2026-08-13, PR #126 https://github.com/jmgirard/intraclass/pull/126)

**Goal:** Replace the pooled "about 6% / about 4% narrower" width figure with
the conditional statement the grids support (flat to ρ = 0.3, parity near 0.6, shrinking in the subject count).

**Outcome:** All seven doc statements (`R/icc.R` ×2, `R/ci-classical.R`,
`R/boundary-hint.R`, both vignettes, `NEWS.md`) carry the three directional
clauses as verbatim fixture-derived templates plus the grid hedge (the smaller
grid's margin shrinks — pinned); the article tabulates both cuts, its
largest-margin claim cut-qualified beside the 11-of-16 paired-cell
contradiction. `test-doc-skew-caveat.R`: directory-walk legs, canonical shapes
with surface-wide ok enforcement (bare level shapes neighbourhood-scoped,
disclosed in the claimed-classes header), pins from `classical-width-by-cell.tsv`,
orphan-row/percent/spelled nets. Harness: 14 fixture + 20 prose mutations,
coverage asserted, control clean.

**Decisions:** guard promise narrowed to declared classes (re-cut gate
2026-08-13; work-log recorded, no D-entry — a flagged maintainer judgment); the largest-margin claim states its cut.

**Review:** five rounds — three defect returns, pass 4 actioned F1 (92,
discarded ok verdicts) and F4 (80, harness mirror gaps), both fixed in round 5
under a logged thrash-rule override; pass 5 actioned nothing; 18 below-bar
findings logged in the Review sections (git); CI green on the merge head.
