# M115: Document the default interval's skew/kurtosis under-coverage — and withdraw the falsified `"burch"` advice

**Status:** done (2026-08-08, PR #124 https://github.com/jmgirard/intraclass/pull/124)

**Goal:** Ship D-028's `document` disposition: name the default Monte-Carlo interval's measured under-coverage under skewed/heavy-tailed subject effects where users meet it, and withdraw the `"burch"` never-under-covers claim the same measurement falsified — including the hint the package prints at runtime.

**Outcome:** `@section Confidence intervals:` in `R/icc.R`, a new `### When the default under-covers` section in `vignettes/interval-methods.Rmd`, and the `glossary.Rmd` Monte-Carlo entry now name the failure surface (worst non-abort coverage 0.6725 at rho 0.60 / k 50 / n 5 / chisq1; held-out lognormal + laplace 0.825 / 0.84 at the same geometry). The falsified `"burch"` claim and its heavy-tail recommendation are withdrawn from seven sites: `R/icc.R` `@param ci_method`, `R/icc.R` Details, `R/boundary-hint.R`'s runtime blurb, `R/ci-classical.R`, both vignettes, and NEWS. Replacement comparison (searle closer to nominal in most cells of every family; burch below-nominal in fewer) is pinned by test and two `out` ledger rows. New `data-raw/make-skew-undercoverage-fixture.R` derives `tests/testthat/fixtures/skew-undercoverage.tsv` from the M113/M114 artifacts; `tests/testthat/test-doc-skew-caveat.R` reads the INSTALLED package (`tools::Rd_db()`, `inst/doc/`, installed NEWS, rendered hint) so the pin runs under `R CMD check` — 169 assertions, 0 skips. No runtime behaviour changed beyond the hint's text.

**Decisions:** none promoted. Two gated amendments: AC1–AC3 re-aimed from source-tree reads to the installed package (a source-reading test skips under `R CMD check`); AC3's line-based grep replaced by a whitespace-collapsed search after mutation showed the grep blind to a wrapped occurrence.

**Review:** Two passes. Pass 1 — three lenses; history and prior-review clean; diff-bug found 20, three actioned: F1 (96) a sixth site still shipping the withdrawn claim, wrapped across lines so both the AC3 grep and the test missed it → defect return 1; F4 (88) and F2 (82), prose claims the fixture contradicts. Pass 2 over the rewrite verified all 17 numeric claims and found 16 more; two actioned: G3 (82) the attribution test was near-vacuous (`"0.6"` matched inside `0.6725`), rebuilt with numeric-boundary matching and mutation-verified; G1 (85) the shipped `"burch"` is *wider* / `"searle"` is *narrowest* claims are backwards (burch narrower in 59/64 cells) — pre-existing, routed to a candidate row. G2 (72) the glossary Burch entry fixed as the same seventh-site pattern.
