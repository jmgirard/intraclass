# M145: The v0.1.0 release-round documentation remainder

**Status:** done (2026-08-28, PR #156 https://github.com/jmgirard/intraclass/pull/156)

**Goal:** Close the documentation debts the v0.1.0 release round left open, so the shipped release notes stand on their own and the spelling gate can fail.

**Outcome:** `NEWS.md`'s *Confidence intervals* section gains an orienting bullet above the "Which of the two closed forms" bullet, naming `"searle"` and `"burch"` as the two classical closed forms and the three grids that measure their widths; the smaller grid's 16 cells and the larger grid's 64 cells are phrased so `test-doc-skew-caveat.R`'s existing `grid_size` and `n_grids` shapes derive both figures from `classical-width-by-cell.tsv` rather than pinning them by hand (GP8), and the third grid's residual-draw description comes from `width-reversal-by-cell.tsv`'s header, its cell count deliberately unstated. All five referring expressions M142's review recorded as antecedent-less now first occur at or after that bullet. *Engines* gains a brms bullet: both random- and fixed-rater models under the sourced half-*t*(4, 0, 1) prior on every random-effect SD, the posterior-mode point estimate with a percentile credible interval and forced `ci_method = "posterior"`, and a custom `prior` warning and voiding the coverage results — all read off `R/icc.R:322-323` and `:553-561` rather than composed. New pin `tests/testthat/test-news-brms-claims.R` slices the *Engines* section out of the INSTALLED `NEWS.md` and holds four tolerant phrase patterns against an anti-vacuity control stated independently of all four; each fact deleted in turn reds exactly one expectation. `tests/spelling.R` flips `error = FALSE` → `TRUE`, so an unlisted word halts the run; `inst/WORDLIST` gained nothing. `tests/spelling.Rout.save` is gitignored — `spell_check_test()` writes it from `spelling`'s own `@INPUT@` template into whatever test directory it runs in, so a local run leaves one in-tree.

**Decisions:** D-043 promoted. Milestone-local: the brms guarantees took their own bullet rather than folding into the existing engine bullet, and the pin took tolerant phrase patterns over verbatim sentences — both chosen at the implement gate.

**Review:** One round, user-facing tier. The three lenses ran in-session rather than in fresh-context subagents: agent delegation was not authorized this session, the same departure the plan phase recorded. All six criteria verified on fresh evidence; gate clean (`cairn_validate` exit 0, `devtools::check()` `Status: OK` 0/0/0 read raw, `devtools::test()` FAIL 0 | WARN 3 | PASS 9122, six `data-raw/` checkers, `document()` no-diff, eleven CI checks). Seven findings; two actioned at the merge gate — the brms bullet said "the random-rater model" while `R/icc.R:325-337` documents three fixed-rater designs too (widened to "both random- and fixed-rater models" and pinned as a fourth claim), and a T6 work-log bullet size of 355 bytes that measures 398 (superseding work-log line, history being append-only). Five rejected: two unreached branches in a pin verified to discriminate, one restatement AC1 required, and the two history lenses found no regression. Nothing retired or graduated.
