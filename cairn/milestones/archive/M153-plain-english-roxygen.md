# M153: Plain-English pass over the roxygen surface

**Status:** done (2026-09-16, PR #171 https://github.com/jmgirard/intraclass/pull/171)

**Goal:** Apply R1 to R8 of `cairn/doctrine/prose-style.md` to every `#'` line outside `@examples` and outside `@noRd` blocks under `R/*.R`.

**Outcome:** The help pages for `icc()`, `d_study()`, `choose_icc()`, `ratings` and `ratings_incomplete` are rewritten under the house prose rules: 717 roxygen sentences over `R/*.R`, 0 dashes, 7 over 25 words, each a clause `test-doc-skew-caveat.R` or `test-occasions-vocabulary.R` pins verbatim (at most 6 words outside the pinned text). Every glossary term is glossed verbatim from `data-raw/glossary-terms.tsv` at its first use per file. `?icc` is titled "Intraclass correlation coefficients for interrater reliability" and `?d_study` "Project reliability to other numbers of raters"; the `M9` ticket id left `?d_study`. `data-raw/mpl-doc-claims.tsv` re-keyed (eleven rows at T2, four `out` rows added for split sentences, two more at review). `data-raw/prose-profile.py` treats `@examplesIf` as an examples block and `data-raw/prose-terms.py` reads `.R` files in roxygen mode, each with a self-test planted red on the old tool. One NEWS Documentation bullet. Nothing computed changes. Baseline before: `R/*.R` 571 sentences / 86 over 25 / 5 dashes.

**Decisions:** none milestone-local. Plan gate: `@noRd` blocks excluded (they render to no `man/` page). Implement gate: AC1 amended to the exemption procedure over the pinned texts, after two fresh-reader rounds and a mini gate; T6 tool fixes accepted as a minor amendment.

**Review:** three-lens fan-out. [S] blame-history: 0 findings, the M113/M117/M118 hedges and the M138 `occasions`/`n_o` conditions reported preserved. [S] prior-review: probe `[]`; 2 findings, both displaced referents after a split (`choose_icc()`'s "They", `?icc`'s "that order"), fixed now. [O] diff-bug: 11 findings. Fixed now: a detached multilevel frame in `?d_study` (R6), "identified" to "told apart by the design", "covered as" back to "defined", "That value" after the parity clause, the placeholder's "because", a docstring line carrying a milestone id. Rejected: the unledgered four-methods sentence (ledger keys by trigger token; the pinning test is named), the gloss placement the first-use rule forces, the "One such / The other" pair (same two members as before), the dropped evidential colon. Follow-up: the term checker's untested examples-skip claim, pruned at hygiene under the §7 chip (the skip lives in the ruler's shared reader, whose self-test exercises it). Gate: `document()` no `man/` drift (the roxygen2 8.0.0 `NAMESPACE` artifact restored), suite 0 failures / 0 warnings / 2 skips, check 0/0/0, pkgdown clean, ledger 64 claims 0 failures. CI green on 11 checks; one Codecov bot comment, noted. Hygiene: one LESSONS line added (a TSV empty field lost to an edit), the M76 line retired (its CI job now fails on the mistake it warned about); M150 rotated out; nothing graduated.
