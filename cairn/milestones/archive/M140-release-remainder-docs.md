# M140: Release-remainder documentation corrections, and the recorded pre-submission check

**Status:** done (2026-08-27, PR #151 https://github.com/jmgirard/intraclass/pull/151)

**Goal:** Correct the four false documentation claims descoped from M48 and re-run `R CMD check --as-cran`, recording that run's own platform in `cran-comments.md` so the submission record and the check that backs it name the same machine.

**Outcome:** Four corrections, no change to what the package computes. (a) `cran-comments.md` now carries the `--as-cran` run's own header — R 4.6.1 (2026-06-24), `aarch64-apple-darwin23`, macOS Tahoe 26.6.2, UTC 2026-08-27 — replacing a `macOS 15` line this machine does not run. (b) The `n_o` NA condition at `R/icc.R:687-694` / `man/icc.Rd`, `NEWS.md`, `R/icc-methods.R:392-396` and `R/icc.R:2513-2523` states the condition `replicates_uniform` (`R/design.R:48-51`) and `multilevel_replicate_facts` (`R/design.R:215-227`) guarantee — within-cell replicates, equal per-cell counts, every cell the design defines present — instead of predicting per-family behaviour. The measured disposition grid: random single-level ragged/missing-cell report `NA`; fixed-rater and multilevel failing shapes abort `intraclass_unsupported` (`R/icc.R:1456-1465`). (c) `NEWS.md`'s report-all sentence is scoped per function; `d_study()`'s vector-valued arguments are the projection axes `m` and `n_o`, mutually exclusive. (d) `R/d-study.R:125-129` / `man/d_study.Rd` say the projection object *gains* `level`/`occasions`, agreeing with the same page's `:54-56` and `:75-79`.

**Decisions:** D-040 (supersedes D-037's `d_study()` enumeration alone).

**Review:** Three-lens fan-out, three rounds. Rounds 1 and 2 each returned on AC2 by a new mechanism of the same shape — a stated `NA` condition true in one design family, false in another (design-level, then rater-treatment) — firing thrash trigger (b); the escalation offer went unspent, the T7 repair dropping the per-family prediction entirely. Round 3: all five criteria met. Three findings fixed at the gate (a false internal comment at `R/icc.R:2518-2519`, a D-040/D-037 attribution slip at `:2623`, and "a design ... is reported as `NA`" at three sites); three deferred to candidate rows (a guard test over the disposition grid, D-038's stale unqualified claim, the fixed-rater abort calling a missing-cell design ragged); four rejected with reason. Defect returns: 2.
