# M149: `intraclass` is citable and reviewer-ready, with its companion paper repo open

**Status:** done (2026-09-10, PR #167 https://github.com/jmgirard/intraclass/pull/167)

**Goal:** Open `jmgirard/intraclass-paper` as the drafting home of the JOSS software paper (D-045) and bring this repo up to JOSS's reviewer checklist: contribution, issue and support guidelines, a citation entry, and a link to the paper repo.

**Outcome:** Nothing the package computes changed. `jmgirard/intraclass-paper` is public with README, CC BY 4.0 `LICENSE.md` and a generic-profile cairn scaffold. Here: `inst/CITATION` (`bibentry` Manual; version from `meta$Version`; year from `Date/Publication`, then `Date`, then the current year; ORCID; `key = "intraclass"`; CRAN URL); `CONTRIBUTING.md` (contribute / report / support, the solo-maintainer stance, build-ignored, linked from the README by absolute URL because the CRAN-rendered README cannot resolve a relative one); README: "on CRAN" note, `install.packages()` above the GitHub command, CRAN-version badge, live `citation("intraclass")` chunk; `DESCRIPTION` `URL:` gains the paper repo; one NEWS bullet. Two CI fixes for `lint.yaml` jobs red on `main` since `d40cdcd`: `data-raw/record-claims.tsv`'s terminal-rows expectation rotated to three rows, and `any::devtools` declared for `checkpoint-guard`.

**Decisions:** none milestone-local; D-045 (venue JOSS, drafting in the companion repo, a never-merged `joss-paper` mirror branch here at submission) was recorded at the plan gate. The follow-through (mirror branch, DOI back-fill) is the "JOSS submission and acceptance follow-through" candidate row.

**Review:** Three-lens fresh-context fan-out (user-facing tier). Blame-history [S] and prior-review [S] zero findings (PR-thread probe empty). Diff-bug [O] twelve findings: fixed at the gate — relative CONTRIBUTING link, NEWS "ships" wording, CITATION footer and the `meta$Date` partial-match year, "two independent oracles" against PRINCIPLES #1's oracle types, CITATION line length; on the maintainer's answers — CRAN badge added (lifecycle badge kept), ORCID and BibTeX key added; rejected — the README's live citation showing the development version on GitHub (the release walk re-knits before the CRAN build) and CONTRIBUTING's stance/exception order. One post-gate spelling flag (`md`, from link text `CONTRIBUTING.md` at line start) fixed by rewording, no WORDLIST change. All five criteria verified fresh; final `devtools::check()` 0/0/0 on `bd24a98`; `cairn_validate` exit 0; 11 of 11 checks green; no defect returns. Two CI-watch ceiling stops: each docs-only resume push re-ran the full pull_request check matrix, so the last two log lines were carried to `main` by docs-only commit after the merge. Hygiene: one lesson added (a CI package supplied only by the restored library cache), the M79 lesson pruned for the byte budget, M146 rotated out of the table and the ledger row rotated with it.
