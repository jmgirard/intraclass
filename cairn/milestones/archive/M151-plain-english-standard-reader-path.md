# M151: The plain-English standard, and the reader path

**Status:** done (2026-09-16, PR #169 https://github.com/jmgirard/intraclass/pull/169)

**Goal:** Extend the house prose standard to plain vocabulary, one idea per sentence and no mannered construction, and apply it to `getting-started.Rmd`, `choosing-an-icc.Rmd`, `glossary.Rmd` and `README.Rmd`.

**Outcome:** `cairn/doctrine/prose-style.md` gains R7 (each `data-raw/glossary-terms.tsv` term glossed verbatim or linked at its first prose use per file) and R8 (a seven-marker `grep -E` sweep, stated as the sweep's extent); R2 tightens to 25 words. `data-raw/prose-profile.py` gains `--limit N`, whole-sentence `--verbose` output, `@noRd` block skipping in `.R` mode and a `--self-test`; its blind-spot list moved from the doctrine to the script header. New `data-raw/glossary-terms.tsv` (34 rows, 31 `term`, 3 `exempt`) and `data-raw/prose-terms.py` (hand-run, `--self-test`) carry the term rule. The four reader-path files are rewritten: getting-started 117 sentences, choosing-an-icc 165, glossary 258, README.Rmd 82, all 0 over 25 words except the two glossary sentences carrying the `test-doc-skew-caveat.R` pinned width clauses, 0 dashes; the choosing article's `type` and `raters` headings became plain questions and `## In short` became `## Summary`. `README.md` re-rendered; one NEWS Documentation bullet. Nothing computed changes. Baseline the ruler recorded before the pass: vignettes 969 sentences / 104 over 25, `R/*.R` 571 / 87, README 69 / 1.

**Decisions:** none milestone-local. Plan-gate choices: 25 words over 30/35; a committed term table with a verbatim-gloss-or-link test over a read-and-judge criterion; extending the hand-run ruler over a new checker (D-021); three milestones (M151–M153) over two. Implement gate: AC2 amended for the pinned clauses, the question-mark marker dropped from R8, AC3 given its checker; the counting clause adopted by the user after two reader rounds.

**Review:** three-lens fan-out. [S] prior-review: one finding (the Burch entry's displaced "that same grid" referent, the M136/M137 shape); PR-comment probe empty. [S] blame-history: no conflict, four low-risk notes all reported preserved. [O] diff-bug: sixteen findings. Fixed now: the two-way gloss narrowing to the complete case, the D-study gloss's referent, the REML gloss widening to unbiasedness, the replicate gloss's dropped "one of", consistency glossed as rank agreement, a hard-coded 95% credible level, the NEWS dangling referent, the MPL gloss's missing correction, and the Burch grid referent (a reorder attempt broke the pinned three-clause run and was reverted). Follow-up: five tool notes (two-sided headings, corpus-tuned patterns, sentence-split order, `@noRd` block merging, bare-assert self-tests) extended the "Three prose-apparatus deferrals" row at hygiene under the §7 chip. Noted: AC5's grep lists three non-scripts. Rejected: the NEWS subheading nit. Gate: `document()` rewrote two `NAMESPACE` import lines under a local roxygen2 8.0.0 against the recorded 8.1.0, untouched by the branch, recorded as a toolchain artifact. Conversation: one Codecov bot comment, noted. Suite FAIL 0 / WARN 0 / SKIP 2; check 0/0/0; CI green on three platforms. Hygiene: one LESSONS line added, the M88 line pruned for the byte budget; nothing graduated or retired.
