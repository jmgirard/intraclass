# M157: Plain-English printed guidance, corrected glossary glosses, and no record IDs in messages

**Status:** done (2026-09-24, PR #175 https://github.com/jmgirard/intraclass/pull/175)

**Goal:** Finish three plain-English fixes that M156 left as candidate rows: the printed guidance, the narrow glossary glosses, and the record IDs in a user message.

**Outcome:** The printed text of `choose_icc()` (walkthrough questions and choice labels, rationale lines, notes) and the notes of `format.icc()` and `summary.icc()` now have no dash as punctuation and no sentence over 25 words. Which note prints, the section order and every number are unchanged. The re-recorded `_snaps/choose-icc.md` and `_snaps/icc-methods.md` change wording only. Five glosses in `data-raw/glossary-terms.tsv` were rewritten: D-study names occasion counts, Monte-Carlo interval says "drawing parameter values", credible interval ties the share to the confidence level, one-way vs. two-way holds on incomplete data, and subject vs. cluster level names both levels. All 45 quoting sites in roxygen, `man/`, the vignettes, both READMEs and `NEWS.md` carry the new text. The `glossary.Rmd` credible-interval entry names `conf_level`. `choose_icc()`'s two-way label and rationale line take the same correction. Four user messages lost internal record IDs: "(M20; ADR-030)" in the `n_o` projection message, "(M9)" in `R/d-study.R`, and "(M5)" and "(spec M9)" in `R/icc.R`. `NEWS.md` has a Documentation bullet, and the doctrine scope paragraph names the printed output.

**Decisions:** new glosses drafted in implement against AC4's properties, and a one-off measurement script for AC1 and AC2 instead of extending `condition-text-profile.R`. None were promoted to DECISIONS.md. Two candidate rows were filed at the plan gate: R2 over the older `NEWS.md` bullets, and R7 misses in `R/data.R`.

**Review:** one round, a three-lens fan-out. The blame-history and prior-review lenses found nothing. The diff-bug lens reported 12 minor findings. Six were fixed at the gate: a broken glossary anchor, a narrowed conflated-level note, the confidence half of the glossary entry, a doubled "projection", autoplot's interval wording, and one vignette frame. Five were rejected with reasons, and one was already resolved. No defect or amendment return. CI was green on 11 checks after one timed-out wait, and the PR had no reviews or comments. Hygiene: one LESSONS line added (`getParseText()` over `getParseData()`), and the M71 and M72 lines trimmed to fit the byte cap. Nothing was retired or graduated.
