# M132: Make the two prose-only `icc()` argument values reachable from the docs

**Status:** done (2026-08-22, PR #141 https://github.com/jmgirard/intraclass/pull/141)

**Goal:** Turn two exported `icc()` argument values a reader can reach only by inference or backticked prose into calls they can run and see.

**Outcome:** `multilevel-designs.Rmd` gains *Declaring the design when the labels are ambiguous* — the shipped `school` table, whose rater labels 1-4 repeat in every classroom, fitted three ways: inference's crossed reading, `design = "nested_in_clusters"` (Design 2, `ICC(A,1)` 0.429, subject level only, one `rater:cluster` term), and `design = "nested_in_subjects"` (Design 3, `ICC(1)` 0.412, `rater confounded`). Its framing now names both occasions for `design`, the label-convention case beside the missing-cell one. `d-studies-and-replicates.Rmd` gains *One projected value, without a projection object*: an evaluated `unit = c("single", "average", 6)` adding the `ICC(A,6)` row (0.710), plus an `error = TRUE` chunk running `abort_fixed_agr_projection()` live. Two tests in `test-vignette-claims.R` back all four calls, pinning `ICC(A,6)` to `d_study(fit, m = 6, seed = 1)` in estimate and both endpoints and asserting the refusal by condition class. Also fixed: four `glossary.html#fixed-vs.-random-raters` links resolving to nothing (pkgdown renders the period as `--`), and `record-claims.tsv`'s `roadmap-terminal-rows` expectation, stale since M131's rotation and reddening `check-references` on the default branch. No package code changed.

**Decisions:** none.

**Review:** three fresh-context reviewers ([O] diff-bug, [S] blame-history, [S] prior-PR-comments), 12 findings: 9 fixed on the branch (an unconditional seed-dependent interval claim; a "the data cannot check this" sentence contradicting `man/icc.Rd` and the code; a component-membership assertion that could not fail; three prose overstatements; three weak or missing assertions), 2 to candidate rows (the `@param design` blurb still disagreeing with the article; the `school` fixture rebuilt verbatim five times), 1 rejected as presentation. All 12 planted perturbations red again after the fixes. Every criterion passed as written; no return. Post-merge hygiene widened the M130 and M131 lessons in place rather than duplicating them, and compressed `LESSONS.md` and `ROADMAP.md` back inside their byte budgets.
