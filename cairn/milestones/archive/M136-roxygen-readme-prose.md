# M136: Roxygen and README prose pass

**Status:** done (2026-08-24, PR #145 https://github.com/jmgirard/intraclass/pull/145)

**Goal:** Apply M134's style rules to every roxygen block and to `README.Rmd`, and make `?icc`'s `design` blurb name the same occasions the multilevel article does.

**Outcome:** Every `#'` line outside `@examples` under `R/*.R` and all of `README.Rmd` rewritten against R1-R6 of `cairn/doctrine/prose-style.md`. Ruler at merge: `R/*.R` 592 sentences / 0 dash-as-punctuation / 2 over-35, `README.Rmd` 65 / 0 / 0 (was 40 / 22 dashes / 4 over-35). The 2 over-35 are the two sentences carrying the 58-word clause `residual_template()` returns, which `tests/testthat/test-doc-skew-caveat.R` matches with `fixed = TRUE` and so admits no sentence break; AC1 binds that count to the carrier count rather than exempting by hand, and the identity is re-derived by importing the ruler's own `strip_roxygen`/`paragraphs`/`normalize`/`sentences` segmentation. `?icc`'s `@param design` now names both occasions for declaring a multilevel design (rater labels that do not mean what the crossing pattern implies; missing cells that leave the pattern ambiguous), closing the M132-review candidate row. `data-raw/mpl-doc-claims.tsv` re-keyed 55 rows to 66 (21 re-pointed, 1 deleted, 12 added) with `assertion` and `disposition` preserved on every re-keyed row; AC2's same-commit clause was verified by extracting all 21 branch commits with `git archive` and running the checker in each. Two NEWS Documentation entries. Nothing the package computes or reports changed.

**Decisions:** none promoted. AC3 — a certification that no rewritten hunk widens a claim — was DESCOPED at the return-3 gate and exits to the ROADMAP candidate row "A mechanical check that a prose pass never widens a claim": three review rounds each found a fresh widening of one shape (a sentence split at a scope-carrying mark that drops the scope) after the previous round's closure had covered the shape before it, so reading does not converge and the exit is on a mechanical predicate, never on a count of instances.

**Review:** four rounds. Returns 1-3 were defect returns; thrash triggers (a) and (b) both fired and composed, and the maintainer took descope. Round 4 ran the full three-lens fan-out over the narrowed set (AC1, AC2, AC4, AC5, AC6): blame-history no findings (M92's struck rater claim, M105/D-022's struck "finite interval on every dataset", D-022's Burch clause and the M117/M118 two-grids scope clause all intact), prior-review no findings (PR-comment probe returned `[]`), diff-bug ten. All five live criteria and the consistency gate passed, so no finding met the return floor. Eight were actioned fix-now at the approval gate and repaired before merge: a `NEWS.md` bullet claiming every touched claim had been checked against the text it replaced (the one thing the descope declined to promise), its broken "the ones that needed it rewritten" clause, a "two grounds" count over three reasons in `@param posterior_summary`, an orphaned "It" in `@param model`, a colon re-attaching the `"lme4"` design list to "data", the same shape in `choose_icc()`, a README "Every design in that list" that dropped the interval guarantee for incomplete data, and a ledger `reason` naming the wrong clause. A stale ROADMAP byte figure was corrected at hygiene. Two findings rejected with reasons. Nothing graduated or retired.
