# M131: Say what each documented method returns, and stop shadowing the shipped dataset

**Status:** done (2026-08-22, PR #140 https://github.com/jmgirard/intraclass/pull/140)

**Goal:** Close the two CRAN-reviewer documentation nits held as a candidate row since the M123/M124 plan gate, both on the Rd surface a reviewer reads first.

**Outcome:** Each of the three `man/*.Rd` pages with a `\usage` block and more than one `\alias` — `choose_icc.Rd` (3 aliases), `d_study.Rd` (7), `icc.Rd` (8) — now carries one `@return` list in its primary roxygen block (`R/choose-icc.R`, `R/d-study.R`, `R/icc.R`) naming the function and every method sharing the page by its full alias, 18 of 18. Each bullet was written against an observed call, and both `tidy` bullets state where the conditional `occasions`/`type`/`level` columns are inserted; `?icc` and `?d_study` close their `\value` with linked `tidy()`/`glance()` generics. `?icc`'s example dropped its hand-built copy of `ratings` (`identical()` to the shipped object) for the shipped dataset plus a `head(ratings)` line showing the long one-rating-per-row layout, leaving 0 example bindings that shadow exported data. Two NEWS Documentation bullets; `.gitignore`/`.Rbuildignore` entries for a stray `run_examples()` `Rplots.pdf`. Every changed `R/` line is a roxygen comment line, so no computed value moves.

**Decisions:** none promoted. Implement gate chose one `@return` list per page over a tag on each method's block, having measured that roxygen2 merges such tags in source-file collation order (a probe tag on `print.icc` rendered above `icc()`'s own paragraph). AC3 and T4 were amended together mid-implementation: `R CMD check --as-cran` on R 4.6.1 emits no `\value`-specific check heading, so AC3 dropped that clause and replaced its `Status:`-line comparison with per-NOTE-heading set containment against the merge-base. Two fresh-context [O] readers audited the amended wording in FULL mode; the first rejected a draft as widening, the second caught a residual absolute no-WARNING bar.

**Review:** Three-lens fan-out (user-facing tier, executable-surface diff). [S] blame-history and [S] prior-review lenses: zero findings each; the prior-review lens confirmed `check-mpl-doc-claims.py`'s scope excludes `@return` blocks. [O] diff-bug lens: nine ranked findings — six fixed on the branch (a false `glance.icc_dstudy()` claim, two lost generics cross-references, an overreaching NEWS sentence, two column-order ambiguities), one fixed at the maintainer's selection (`head(ratings)`), one rejected with reason (the `Rplots.pdf` ignore entries, kept), one already answered by re-running the check. No return floor trip; no defect returns. Nothing retired at hygiene; one LESSONS line added and the stalest pruned to hold the byte budget.
