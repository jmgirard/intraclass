# M124: Demonstrate the exported surface no vignette shows

**Status:** done (2026-08-17, PR #133 https://github.com/jmgirard/intraclass/pull/133)

**Goal:** Give the exported methods that no vignette currently shows — `summary()` on an icc, and the whole tidy/plot path off `d_study()` — a worked demonstration.

**Outcome:** `vignettes/getting-started.Rmd` runs `summary(fit)` and shows its interpretive notes. `vignettes/d-studies-and-replicates.Rmd` runs `tidy(proj)`/`glance(proj)` on a `d_study()` result, names `m` as the one column the D-study tidy carries that `tidy()` on the fit does not, passes `seed = 1` to `d_study()` while naming `conf_level`/`mc_samples`/`seed`, and shows `plot(proj)` in an `eval = FALSE` chunk (figure count stays 3, matching `main`). `autoplot.icc_dstudy` gains an `@examplesIf rlang::is_installed(c("ggplot2", "glmmTMB"))` example, so `man/d_study.Rd` carries a guarded and an unguarded block. Three NEWS Documentation bullets. No computed value or exported behavior changed.

**Decisions:** none promoted. One gated amendment: AC2's parenthetical named `type`/`level`/`occasions` as columns the D-study tidy adds over the icc tidy; measurement falsified all three (`type`/`level` are unconditional in `tidy.icc`; `occasions` is gated on the same `isTRUE(design$replicates)` predicate on both sides), so the criterion narrowed to `m`, the only one, verified across 11 fit/projection configurations.

**Review:** One pass, three-lens fan-out (user-facing tier). Both [S] lenses clean — blame-history confirmed `autoplot.icc_dstudy` never carried an example that was later removed, and the prior-review lens found no M123-withdrawn wording reused (its `pulls/comments` probe returned `[]`). The [O] diff-bug lens returned F1–F9; F1–F8 fixed on the branch at the gate, F9 rejected. The substantive ones: `seed = 7` made the `m = 4` row's interval diverge from the fit's, breaking the comparison the anchor prose invites (fixed to the fit's own `seed = 1`, verified byte-identical to inheriting it); "a second copy of the curve above" was false (`m = 1:12` vs `m = 1:8`); a `###` heading swallowed the rest of the section; "each default to the value stored on the fit" is false for `mc_samples`, which falls back to a hard-coded 10000 on a non-Monte-Carlo fit; "can and cannot separate" where only the negative note is emitted; and a NEWS claim false off the one-way branch. F4's companion claim that `seed` inherits `NULL` was falsified by measurement — a fit seeded 3 yields a projection seeded 3 — and is recorded as such. Nothing retired from LESSONS; the M70/M110/M118 pin-a-list-procedurally family was extended in place with AC2's instance, the file being at its line cap.
