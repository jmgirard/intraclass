# M152: Plain-English pass over the method articles

**Status:** done (2026-09-16, PR #170 https://github.com/jmgirard/intraclass/pull/170)

**Goal:** Apply R1 to R8 of `cairn/doctrine/prose-style.md` to `engines.Rmd`, `comparison-with-other-packages.Rmd`, `d-studies-and-replicates.Rmd`, `multilevel-designs.Rmd` and `interval-methods.Rmd`, and reshape `interval-methods.Rmd` so each method section opens in plain words.

**Outcome:** The five method articles are rewritten under R1 to R8 at the 25-word ruler: 721 sentences, 0 dashes, 2 over 25 words (both in `interval-methods.Rmd`, carrying the `width_templates()` flat clause and the `residual_template()` clause verbatim, 3 and 7 words outside). Each of the five `ci_method` sections in `interval-methods.Rmd` opens with a paragraph on what the method is for and when to choose it; all 8 base headings and every prose numeral kept. Every glossary term is glossed or linked at its first use per article (`prose-terms.py` exit 0); the R8 and identifier greps are empty. The comparison article says in one sentence what `psych`, `irr` and `irrICC` are; the multilevel article defines Design 1 in plain words at its first label. Three `mpl-doc-claims.tsv` vignette rows and two `m117-width-pin-mutations.R` anchors re-keyed to the split sentences. One NEWS Documentation bullet. Code chunks and pasted output byte-identical to base; nothing computed changes.

**Decisions:** none milestone-local. Plan gate: restructure `interval-methods.Rmd` with plain openings over a sentence-level pass. Implement gate: AC1 amended to M151's pinned-clause wording because the three `width_templates()` clauses must sit in one contiguous run and the flat clause admits no break. Tool note pruned at hygiene (§7 chip): `prose-terms.py` slugs headings the pandoc way (`fixed-vs.-random-raters`) where pkgdown links use `fixed-vs--random-raters`, so such links read as unlinked and the pass glossed in the sentence instead.

**Review:** three-lens fan-out. [S] blame-history: no conflict with the M117/M118/M119 pinned claims, the D-017/D-014 MPL fences, the M135 matrix or the M137 repairs. [S] prior-review record: no M151/M136/M137 regression; PR-comment probe empty. [O] diff-bug: twelve findings. Fixed now: the Monte-Carlo opening promising opt-in coverage on every aborting design; the intro's mpl sentence dropping the balanced, complete, random, absolute-agreement restrictors; the npbootstrap opening claiming the whole one-way terrain; the searle/burch opening widening burch's skew under-coverage to both; the engines intro asserting convergence of any different estimator; the multilevel example turned into a definition; the NEWS "defined in plain words" where three uses are links; `seed` code formatting. Rejected: a see-also label losing "boundary-aware", the true `irr::icc` ANOVA sentence, line-wrap width. Noted: AC5 unticked at read time. Gate: the same `NAMESPACE` roxygen-version artifact M151 recorded, restored. Conversation: one Codecov bot comment, noted. Suite FAIL 0 / WARN 0 / SKIP 2; check 0/0/0; CI green on eleven checks. Hygiene: one LESSONS line added, one pruned for the byte budget; nothing graduated or retired.
