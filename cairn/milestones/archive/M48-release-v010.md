# M48: v0.1.0 release consolidation — CRAN submission-ready

**Status:** done (2026-08-26, PR #147 https://github.com/jmgirard/intraclass/pull/147)

**Goal:** Consolidate the post-M44–M47 package state into a CRAN-submission-ready v0.1.0 (the upload itself stays the maintainer's out-of-band act, per ADR-022).

**Outcome:** `DESCRIPTION` carries `Version: 0.1.0` and an honest `R (>= 4.0.0)` floor (rlang's own `Depends` is the binding constraint); `NEWS.md` is a single `# intraclass 0.1.0` changelog. The exported-API last call (RB04/RR04) shipped seven accepted changes: scalar `raters`/`posterior_summary` defaults, with `validate_choice()`'s `identical(value, choices)` shortcut removed so every choice argument across `icc()`, `d_study()` and `choose_icc()` aborts classed on a multi-valued value; identifier columns always present in `tidy.icc()`/`tidy.icc_dstudy()`, `NA` where inapplicable, so the two schemas row-bind; `index` renamed `term` for broom interop, swept through vignettes, `data-raw/` and tests; `var_subject_rater`, `n_o`, `rhat` and `ess_bulk` added to `glance.icc()` so `var_residual` never silently changes meaning on a replicate fit; the `icc` object's public/internal boundary stated in `@return`; classed validation of `d_study()`'s `conf_level`, `mc_samples` and `seed`. `glance()$n_o` fixed on nested replicate designs. New contract pin: `tests/testthat/test-exported-contract.R`. `DESIGN.md`'s Platforms commitment corrected to the matrix CI actually runs, with the untested-floor tension recorded under Known issues.

**Decisions:** D-035, D-036 and D-037 promoted (the last classifying choice arguments by (function, argument) pair rather than by argument name). Milestone-local: the release ran as milestone tasks rather than through `/cairn-release` (plan gate); AC3 was descoped at the third-pass triage gate with its number held, its `cran-comments.md` environment line and four documentation findings exiting to a ROADMAP candidate row that lands before the upload; O1's repair was taken as the documentation widening, not a change to a shipped column at the API last call.

**Review:** Four passes. Three defect returns (F1/F2 on the first, AC3's spelling on the second, AC3's `cran-comments.md` environment on the third) and one amendment return (AC7); the thrash rule's threshold (a) was reached and answered by descope rather than a fourth repair round, with (b) firing too — AC3 failed twice, each time on a hand-authored release-artifact fact no check the milestone runs can catch. Fourth pass, full three-lens fan-out: the prior-review lens found no regression and zero findings; the blame-history lens eight, all restatements of the descoped claims plus the D-037 blast radius; the diff-bug lens ten. Consolidated as P1-P12 — **P5 fixed at the gate** (five vignette tables labelled a display column `index` while sourcing `$term`, an inconsistency the rename sweep introduced; the `print.icc()` transcripts keep `index`, which that printer still emits); **P3, P4 and P6 to candidate rows** (`glance.icc()` reports no `raters`/`type`/`replicates`, so rater treatment has no stable accessor; its newly frozen schema has no exact-names pin where `tidy()` does; `occasions` has an unstable ptype across fits); seven rejected as already carried by candidate rows or by the recorded descope, one style nit rejected. Six live criteria verified fresh: `--as-cran` 0 errors / 0 warnings / 0 notes, installed-package suite FAIL 0 | PASS 8592, `pkgdown`/`lintr`/`air`/`urlchecker` clean, ten of ten PR checks green. Nothing retired or graduated.
