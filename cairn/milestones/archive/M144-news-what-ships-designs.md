# M144: NEWS's *What ships* names the designs the package actually supports

**Status:** done (2026-08-28, PR #155 https://github.com/jmgirard/intraclass/pull/155)

**Goal:** `NEWS.md`'s *What ships* section names the design support `DESCRIPTION` sells and the package delivers, which M142's condensation left out.

**Outcome:** Two bullets added to *What ships*, no code change. The first carries `DESCRIPTION:13-14`'s phrase verbatim, "support for imbalanced, incomplete, and multilevel (nested) designs", glossed as unequal ratings per subject, missing ratings, and subjects nested in a higher-level unit, and states the cluster level's condition: a `cluster` column on a two-way design adds a cluster level when the same raters span every cluster (Designs 2 and 3 report the subject level only). The second names within-cell replicates: on a two-way random design `icc()` splits the single-rating residual into a subject-by-rater interaction and pure error, and `occasions` reports one rating or, on balanced replicates, the mean. Both end with the `d_study()` bullet's house pointer to where estimation refuses. Bullets measure 481 and 413 bytes against M142's 500-byte cap; the file's rulers are unchanged at `dash: 0`, one sentence over 35 words. This closes item (b) of the "What the 0.1.0 NEWS entry no longer says" candidate row; items (a), (c), (d) stay open under the maintainer's M142 omission-over-restoration direction.

**Decisions:** none. Two plan-gate choices stand as recorded: AC1's domain derived from `DESCRIPTION`'s own clause rather than the candidate row's four-item list (the row's "sells all four" is false), and whole-file ruler figures rather than diff-scoped ones (neither instrument enumerates edited bullets).

**Review:** All four criteria passed on fresh evidence first time through; no defect return. Gate clean: `cairn_validate` 16/16, `--as-cran` 0/0/0, all six `data-raw/` checkers, `document()` no diff, pkgdown clean, 11/11 CI. The user-facing three-lens fan-out returned nine findings. The [O] lens found the added prose asserted cluster-level reporting, the residual split, and `occasions = "average"` unconditionally where `icc()` refuses each for named designs — the M142 round-1 defect class, confirmed at review against `R/icc.R:259-260`, `:1400-1442`, `:1917-1921` and `vignettes/multilevel-designs.Rmd:120-123`. Findings 1-6 and 8 fixed at the merge gate and re-measured (M142's re-measure lesson), including a work-log byte figure that recorded 393 where no measurement of the committed text yields it. Finding 7 was mid-review AC-fencing state. The [S] blame lens confirmed the change is the authorised item (b), not a reversal of M142's intent; the [S] prior-review lens found no regression, its `gh api .../pulls/comments` probe returning empty. Nothing graduated or retired.
