# M130: Back the interval-methods vignette's claims, and read it through

**Status:** done (2026-08-22, PR #139 https://github.com/jmgirard/intraclass/pull/139)

**Goal:** Give `interval-methods.Rmd` — the 374-line article carrying every `ci_method` claim, and the only live-code vignette with no claims test — the numeric backing and the editorial read the other six already have.

**Outcome:** 18 `test_that("interval-methods.Rmd: …")` blocks in `tests/testthat/test-vignette-claims.R` back the 40 lines the quantifier sweep returns, partitioned 28 backed there / 9 backed by existing expectations elsewhere / 3 incidental. Reading the article through found three claims false, all corrected: the Monte-Carlo and parametric-bootstrap upper bounds do not coincide (only ICC(A,k)'s round alike at the table's two decimals, and they do not all fall on the same side); the `"lavaan"` engine is not Monte-Carlo-only (it bootstraps complete data, its two-level route needing balanced clusters and random raters besides); and `"searle"` at zero between-subject variance returns the attained minimum only for the single-rater coefficient, the averaged projection reporting `-Inf` through the Spearman-Brown pole. `?icc` carried that same false sentence and is corrected to match. The glossary link `#confidence-interval-vs.-credible-interval` resolved to nothing — pkgdown emits `vs--` — and is fixed; *consistency*, *REML* and *variance component* are linked at first use. NEWS records all of it.

**Decisions:** none cross-cutting. Milestone-local: AC1 amended at a mini gate to admit six dispositions rather than four (23 of the 39 swept lines fit none of the original four); AC3's domain read as the 12 `icc()`-calling tests, the `ci_method` perturbation form being undefined for the six fixture readers.

**Review:** three passes. Passes 1 and 2 each returned the milestone (17 and 16 findings; the floor-qualifying ones were two still-false article claims, an untested lavaan balance fence, a half-asserted two-sided claim, an unswept AC3 domain, and a missing NEWS entry). Pass 3: all six criteria pass on fresh evidence — AC1 40/40 ledgered verbatim, AC3 re-verified by a 10-plant replay, AC4 17/17 targets against a site rebuilt from scratch, suite 0 failures / 8500 passing, `R CMD check` level with main at raw `Status: 2 NOTEs`. Three lenses returned 13 findings: 7 fixed at the gate, 2 rejected as out-of-scope, 3 noted, 1 self-resolved. CI went red once on `check-references` — the corrected `?icc` sentence re-keyed the M94 doc-claims ledger — and was re-triaged on the branch. Hygiene: one M130 lesson added; the M78 lesson trimmed to its remainder.
