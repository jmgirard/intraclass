# `npbootstrap` on the skew grid — frozen disposition rule over the M111 cells (M121)

**Provenance.** Ingested 2026-08-15 by M121 from the committed M111 sweep
artifacts (`data-raw/m111-fallback-results.rds`, generated by
`data-raw/m111-fallback-sweep.R`) and the frozen precedent pages
`mc-skew-response-comparison.md` (M113), `classical-oneway-comparison.md` (M76)
and `npbootstrap-oneway-comparison.md` (M62); no shelf source of its own.
Pagination: —.
Extraction: derived — no external source of its own, only as current as its inputs, none re-read since 2026-08-15 — observed 2026-08-15.

**Type.** Synthesis note (M121). Pre-registration first (this file, committed
*before* any harness code or derived table exists — GP5); Results and
Disposition filled at T6 from the committed table.

**Scope.** The pre-registered disposition rule for `ci_method = "npbootstrap"`
on the 64-cell one-way skew grid the M111 fixture holds, and the per-cell
coverage table it is read against. Not a source summary; it builds
no new estimator and changes no user-facing behavior — any wording that follows
from the verdict is a separate milestone (the M113 → M115 shape). This is a
reference, not an authority — status lives in `ROADMAP.md`, decisions in
`DECISIONS.md`, architecture in `DESIGN.md`.

**Evidence snapshot.**

- `data-raw/m111-fallback-results.rds` — committed M111 fixture; per-rep
  endpoints for the `mc`, `searle` and `burch` legs over 64 cells × 2000 reps,
  and no `npbootstrap` leg: this milestone's harness regenerates the replicates
  from their recorded seeds and adds that fourth leg — observed 2026-08-15. <!-- check: ! git grep -qF "npbootstrap" -- data-raw/m111-fallback-sweep.R  # text-level proxy: the generating script runs no npbootstrap leg, so the fixture holds none; the rds is binary and the check-references job is R-free by design (.github/workflows/lint.yaml) -->
- The M76 `npbootstrap` figures quoted under Known priors — `data-raw/m76-sweep-results.rds`,
  `$summary` rows with `method == "np"` — observed 2026-08-15. <!-- check: git grep -qF "ci_method = \"npbootstrap\"" -- data-raw/m76-coverage-sweep.R  # text-level proxy: the M76 sweep runs the npbootstrap leg whose per-cell rows carry these figures; same R-free constraint as above -->
- The three incumbent legs' floor counts quoted under Known priors —
  `data-raw/m113-skew-response-coverage.tsv`, the committed M113 table over this
  same grid — observed 2026-08-15. <!-- check: python3 -c "import csv,sys;rows=[r for r in csv.DictReader((l for l in open('data-raw/m113-skew-response-coverage.tsv') if not l.startswith('#')),delimiter='\t')];c=lambda m,col:sum(1 for r in rows if r['method']==m and float(r[col])<0.93);sys.exit(0 if (c('searle','coverage_uncond'),c('burch','coverage_uncond'),c('mc','coverage_uncond'),c('mc','coverage_nonabort'))==(21,16,49,36) else 1)" -->

## What is being decided

D-027 judged three interval methods against a 0.93 per-cell floor on this grid
and recorded no replacement: `searle` and `burch` both no-GO, the `mc` incumbent
`warn`. `npbootstrap` — the ukoumunne2003 transformed bootstrap-t shipped at M75
— was never run on it. M121 measures that fourth leg on the same 64 cells and
records its disposition, so the repo can say for a fourth method what D-027
established for three.

The disposition is a recommendation to the maintainer gate and nothing more. The
default-method contract is D-001-fenced and is not traded here; a `replace-GO`
would commission a follow-on milestone, never a change in this one.

## Pre-registered disposition rule — frozen 2026-08-15, before any harness code or derived table (GP5)

**Known priors (authored with these in hand).** Three bodies of evidence about
this leg and this grid already exist, and the rule below was written knowing all
of them:

- **`npbootstrap` on the narrower M76 grid** (16 cells: ρ ∈ {0.05, 0.10},
  k ∈ {10, 30, 50}, n ∈ {2, 5}, gaussian and t5, n_rep = 2000): unconditional
  coverage 0.9245–0.9495 with **zero aborts at every cell**, dipping below 0.93
  at exactly one — 0.9245 at (ρ = 0.10, k = 10, n = 2, t5). That grid carries no
  chisq1 and no ρ above 0.10, so it says nothing about the skewed high-ρ corner
  where the other three legs fail worst.
- **The published anchors** (ukoumunne2003 Table I, gaussian, ρ = 0.05, n = 10):
  0.938 / 0.944 / 0.9395 at k = 10 / 30 / 50 — the reducer-level oracle AC2
  re-runs as a precondition, not evidence about the swept domain.
- **The three incumbents on this exact grid** (M113 table, D-027): below the 0.93
  `coverage_uncond` floor at 21/64 cells (`searle`), 16/64 (`burch`) and 49/64
  (`mc`; 36/64 on `coverage_nonabort`), worst cells 0.674 / 0.6655 / 0.3935.

What is genuinely blind is the whole of `npbootstrap`'s 64-cell table: no cell of
this grid has ever been run with that method.

**Tag vocabulary for the verdict (filled at T6).** `replace-GO` | `no-GO`,
reusing D-027's S1 vocabulary unchanged so the fourth leg is read against the
same words as the first three, and so D-027's reopening condition — "a method
demonstrating every-cell floor coverage over this grid" — reads directly off the
tag. `replace-GO` means the floor is cleared at every cell and the replacement
question is reopened for the maintainer; it is never itself a default change.

| # | Rule (binding unless marked) | Source of the threshold |
|---|---|---|
| N1 | **Floor bar for `npbootstrap`.** The leg is `replace-GO` iff its per-cell **`coverage_uncond`** — the unconditional coverage column of `data-raw/m121-npbootstrap-skew-coverage.tsv`, over all 2000 replicates of a cell including those where `icc()` signalled a classed `intraclass_*` condition — is ≥ 0.93 at every one of the 64 cells; otherwise `no-GO`. Reported alongside, descriptive and never binding: the failing cells partitioned by abort rate at 0.1, with `coverage_nonabort` for each part; the worst cell with its (ρ, k, n, dist); and the median width ratio against the `mc` leg. | floor: M76 C2 (ukoumunne2003 worst tabulated 0.931; burch2011 §5 "just under 0.93"); every-cell form and the unconditional column: D-027 S1 |

**Why the unconditional column.** D-027 judged `searle` and `burch` on
`coverage_uncond` and this leg is being compared to them, so the column is
inherited rather than chosen. The abort-rate split is reported because D-026
already adjudicated the abort event as informative: a failing cell above the 0.1
abort boundary is that adjudicated selection effect, not a fresh finding, and the
split is what lets a reader tell the two apart without moving the bar.

**Aggregation (frozen).** N1 yields exactly one tag, read directly off the
derived table. Whether D-027's S1 reopening condition is met follows from that
tag and is stated explicitly in the same D-entry. The verdict — one tag plus the
reopening statement — is recorded as one D-entry, and no bracketed claim token is
minted for it.

## Results — derived 2026-08-15 from the M121 sweep

Backing table `data-raw/m121-npbootstrap-skew-coverage.tsv` (256 rows = 64 cells
× 4 legs), written by `data-raw/m121-npbootstrap-skew-sweep.R`; every figure
below re-derives by re-running that script, whose own preconditions (the Table I
anchors, the endpoint identity check) gate the run.

- **N1 `npbootstrap`: no-GO.** 26/64 cells below the 0.93 `coverage_uncond`
  floor; worst 0.8730 at (ρ = 0.60, k = 10, n = 5, chisq1). Coverage spans
  0.8730–0.9555 across the grid.
- **The abort-rate partition is degenerate, and that is the finding.** The leg
  signalled no classed `intraclass_*` condition on any of the 128,000
  replicates, so every cell's abort rate is 0, all 26 failing cells fall in the
  ≤ 0.1 part, the > 0.1 part is empty, and `coverage_nonabort` equals
  `coverage_uncond` at every cell. D-026's selection effect therefore explains
  none of these failures — unlike the `mc` leg, where 31 of its 49
  `coverage_uncond` failures sit above that same 0.1 abort boundary (D-027
  states the phenomenon on its own column, as 26 of the 36 cells failing
  `coverage_nonabort`). Each of these 26 is a coverage failure on its own
  terms.
- **Where it fails.** By generator family: chisq1 11/16, t5 8/16, uniform 4/16,
  gaussian 3/16 — so the failures are not confined to one nameable family. By
  design: 16 of the 26 sit at k = 10 (10 of them at the k = 10, n = 2 corner),
  6 at k = 30, 4 at k = 50. Within chisq1 the failure deepens with ρ, from
  0.9260–0.9425 at ρ = 0.05 to 0.8730–0.9040 at ρ = 0.60.
- **Its failures are far shallower than the other three legs'.** On the same
  grid and column: `mc` fails 49/64 with a worst of 0.3935, `searle` 21/64 with
  0.674, `burch` 16/64 with 0.6655. `npbootstrap` fails more cells than either
  classical leg, and its worst cell still covers 0.873. Fewest-failing and
  shallowest-failing are different orderings, and N1 reads neither: it is an
  every-cell bar and all four legs miss it.
- **Width (descriptive).** Median width ratio vs the `mc` leg 1.047 (range
  0.718–1.720); by family, chisq1 1.355, t5 1.154, gaussian 1.017, uniform
  0.917. The leg buys its comparatively shallow under-coverage with intervals
  that run wider than the incumbent's exactly where the incumbent fails worst.

## Disposition

Verdict recorded as D-031: `npbootstrap` **no-GO** on N1 — no default-method
change, and the D-001 fence untouched. D-027's S1 reopening condition (a method
demonstrating every-cell floor coverage over this grid) is **not met**: 26 cells
sit below the floor, so the classical-replacement question stays closed on the
evidence D-027 recorded.

What this milestone does not decide: whether a leg failing 26 cells at a worst
of 0.873, with no aborts anywhere, warrants any user-facing wording. That is the
M113 → M115 shape and belongs to a follow-on milestone, not to this rule.

## Open questions

None — the rule was read exactly as frozen, and the zero-abort result made its
abort-rate clause degenerate rather than ambiguous.

