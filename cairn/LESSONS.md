# Lessons

Durable repo lessons — build quirks, testing tricks, gotchas worth
remembering next time — captured at milestone end and surfaced at plan time.
Not status, not decisions: a lesson is a reusable "how this repo actually
behaves" note. Cross-cutting *choices* still go to `DECISIONS.md`.

Append-only; one line per lesson: `- YYYY-MM-DD (M<NN>): <lesson>`. Capped at
50 lines — when full, prune the stalest lessons rather than letting it grow;
git history keeps the full record.

<!-- lessons appended below by /milestone-review post-merge hygiene -->
- 2026-07-18 (M62): `lintr::lint_package()` lints `data-raw/` too, and `object_name_linter` rejects UPPERCASE constants (`N_REP`, `B_BOOT`) — `air format --check` does NOT catch it, so a new data-raw script passes air and still reds the CI lint job. Run lintr locally before pushing one (renaming to snake_case is numerics-safe, but re-run the script to catch a missed call site). **(M95, extended 2026-07-31: the CI lint job can also red on lintr VERSION drift — CI's newer lintr flags `<<-` via `assignment_linter` while a green local `lint_package()` stays silent; treat local lint as necessary, not sufficient, and read the CI annotation for the linter name.)**
- 2026-07-18 (M62): when benchmarking against an incumbent that can ABORT (`intraclass_singular_fit`), report `n_ok` beside coverage — coverage conditional on the non-aborted reps is the accounting most FAVORABLE to that incumbent, so it is the conservative choice for a "not worse than incumbent" claim (and the absolute nominal floor, not the incumbent-relative clause, should carry the verdict or the comparison turns circular).
- 2026-07-18 (M63): `cairn/references/sources/` (named `pdf/` until the M68 rename) is a LIVE directory the maintainer adds to mid-session, so an absence recorded as durable fact ("source X has no PDF") can go stale within the hour — M63 correctly found 29 PDFs and asked for the missing O-SEM source; the maintainer supplied it ~2 h later, after which a review lens read the now-stale record as an implement error. Re-verify any absence claim at review before acting on it, and prefer recording absences as a dated observation, not a standing fact. To date when a file actually arrived (mtime is preserved by a copy and lies), compare `stat -f %Sc` ctimes: a batch shares one ctime, a later addition stands apart.
- 2026-07-18 (M63, folded with M61 2026-07-31/M94 cap): `gh pr create`, `gh pr checks --watch`, and `gh pr merge` all use GraphQL (shared 5000/hr budget) — three review subagents' gh calls exhausted it and blocked both PR creation and the merge mutation. Poll CI via REST (`gh api .../commits/{sha}/check-runs`; /rate_limit is exempt), create PRs via REST (`gh api --method POST repos/{owner}/{repo}/pulls --input <json>`, building the JSON with `jq -n` for body escaping), and keep the GraphQL budget for the merge.
- 2026-07-18 (M64): a source note's `## Open questions` can be invalidated by a LATER task in the same milestone — two of three actioned review findings were notes still escalating gaps (`no BIBLIOGRAPHY entry`, `sibling-key hygiene`) that T5 had closed hours earlier, plus stale grep claims in their `## Traces to`. When a milestone both writes notes and edits the files those notes describe, re-read every note's cross-file assertions after the editing task lands; a subagent's note is a snapshot of the repo at write time, not at merge time. **(M64, reinforced M65: it recurred anyway — `saha2012.md` shipped "the PDF has not been read at the time of writing" about a paper `saha2005.md` had reconciled in the SAME task. Prose intent is not enough; make it mechanical — before the review commit, grep the new notes for time-relative phrases (`at the time of writing`, `not yet`, `must be checked`, `when that note is written`, `not retrieved`) and re-resolve each hit.) (M66, refined: it recurred a THIRD time and the sweep caught it — but the pattern must also cover `today` and the boilerplate `Traces to` lead sentence, both of which slipped past it to review.)**
- 2026-07-18 (M65): never write "not present in the PDF" / "not retrieved" about material you did not open — M65 claimed `mehta2018`'s Appendices A–C were absent and flagged them unchecked; all three were on pp. 2750–2752 of the shelf PDF, one Read call away, and they held the tables substantiating the paper's central safeguard claim. A reference list is NOT reliably the last page (Wiley puts appendices after it). Before recording any absence, read to the final page; if you truly cannot check, say "not checked" and never also assert what is or isn't there.
- 2026-07-18 (M64): three of ten shelf PDFs were NOT the version of record (`tenhove2022`/`tenhove2024` advance-online with © years 2021/2022 and no journal pagination; `tenhove2020` an author manuscript with no publisher fields), so a bare "p. 6" anchor is ambiguous and a "journal p. N" for a paper starting at p. 650 is impossible — state the pagination basis in the note and convert explicitly. To verify a claim inside a figure too small to read in a full-page PDF render, crop at high DPI: `pdftoppm -r 300 -f <pg> -l <pg> -png -x <X> -y <Y> -W <w> -H <h> file.pdf out`. **(M66, extended 2026-07-19: a shelf preprint may have a published version of record under a DIFFERENT FIRST AUTHOR — `jorgensen2019` turned out to be an author manuscript of `vanderark2023`, same study to the digit, byline reordered — so a citekey built from a preprint byline is unfindable from the published record, and the published version may carry whole sections the preprint lacks (here: a per-cell results table, two figures, and the R+Stan appendix). When a manuscript prints no year/venue, treat the citekey as provisional and search for the published chapter before relying on the note. Also: do NOT citekey a Dutch-authored source from the publisher's citation generator — Springer renders the tussenvoegsels as "Ark, L.A.v.d." / "Hove, D.t.", which would give `ark2023`; the chapter byline reads "van der Ark" / "ten Hove", so file under V and t as the existing `tenhove*` keys already do.)**
- 2026-07-18 (M68): never reword a tracking record to silence a `cairn_validate` advisory — fix the fact or accept the WARN. ORACLES.md's extraction status was changed from "none re-read at the split" to "none re-read since 2026-07-18" to clear an advisory; the new wording implied a read D-007 says never happened and made the repo's most load-bearing page (every oracle value traces there) escape the 180-day staleness WARN until 2027-01-14. An advisory that goes quiet because the text changed rather than the world is a hidden defect, not a pass.
- 2026-07-18 (M68): write an acceptance criterion as the PROPERTY it protects, never the surface FORM it expects — "every Extraction status reads unverified-first-pass" and "the 5 non-shelf pages read `derived —`" each cost a review send-back, the second *because* the actioned review fixes corrected two pages and falsified the enumeration. A criterion that pins a form breaks whenever a fact is corrected, which is exactly backwards.
- 2026-07-18 (M69): read a fixture/helper end-to-end before ruling on what it claims — `helper-shrout-fleiss.R` carries an ACCURATE provenance header at the top and the opposite claim in a second comment block at line 72. Checking only the header produced a wrong "already accurate" verdict at review, caught by an independent reviewer citing the line.
- 2026-07-19 (M66): a string absent from a PDF's TEXT LAYER may still be PRINTED on the page — `trevethan2017`'s `Published online:` footer is absent from `pdftotext` in `-layout`, `-raw`, AND whole-document modes, yet is plainly printed on p. 1 beside the Springer logo; an independent [O] reviewer AND the [S] scorer both concluded (scoring it 92) that the note had fabricated it. Settle any "not in the PDF" claim with a high-DPI crop render (`pdftoppm -r 400 -f N -l N -png -x X -y Y -W w -H h`), never the text layer alone — this escalates M69's grep-undercounts lesson from a counting error to an absence error, and a false absence is the more dangerous of the two. Converse, same milestone: 3 of ~40 quotations marked verbatim were paraphrases, so re-read every quoted string against the source before stamping an extraction `verified`. **(M67, reinforced twice in one milestone: the quotation defect recurred at BOTH review attempts — attempt 1 found two altered quotations, and after a fix-sweep whose own work log called it done, attempt 2 found two more, one of them in a note the sweep never opened. A quotation sweep must enumerate every quoted string in EVERY note the milestone touches, not just the notes a prior finding named; "I swept the surrounding quotations" is not evidence unless the sweep was mechanical and per-note.)**
- 2026-07-19 (M70): never pin an item COUNT in a verification record you will keep editing — a note's Extraction line said "the single quotation confirmed" / "all 12 quotations" / "all 11 quotations" while the same pass ADDED quotations, drifting to a true 9/20/15 that only the end-of-milestone consolidated sweep reconciled. Say "every quoted string swept", not a number; a count is a form that breaks when a fact is added (the M68 pin-the-property-not-the-form lesson, in a new place). Corollary: run the mechanical markup/verbatim sweep once more at milestone end over EVERY touched note — a per-task check written before a later task adds a quotation misses it (T1's own added quote carried markdown only T7 caught).
- 2026-07-19 (M71): a claim about the REPO'S OWN STATE ("no document names X", "only Y has been swept") or one GENERALIZING from cited cells to a range/count/superlative fails verification-by-reading in a way transcribed values do not — M71 took FOUR review attempts, and every numeric correction survived independent verification while every failure was interpretive prose, twice in prose written to fix the previous cycle's prose. The remedy is mechanical, not more care: carry the settling command inline in the claim, and recompute a table generalization over the FULL table before writing it (M73/M74 make both routine). **(M104, extended 2026-08-05: the same shape crosses METHODS, not just cells — a correction to the `"npbootstrap"` docs acquired a causal clause ("a resampled limit can land on either side") true of the parametric `"bootstrap"` this milestone measured and false of npbootstrap, which aborts at that boundary and lands strictly below just off it. A sweep of method A is not evidence for method B's doc section; scope a doc correction to the method whose section it sits in, and check the neighbouring method live before borrowing a mechanism.)** A count describing a fixed source table is fine; a count of items the pass is processing is the M70 drifting-tally trap.
- 2026-07-19 (M71, corrected 2026-07-25/M93): merging the default branch INTO a feature branch — exactly what tracking-rules requires when main moves under an active branch — is denied by cairn's merge_guard, which pattern-matches the branch name. BOTH `git merge main` and `git merge origin/main` are now denied; M71's remote-tracking-ref workaround no longer passes (verified twice at M93, with and without a `-m` message, so the message text is not the trigger). Merge by resolved SHA instead: `git merge --no-edit $(git rev-parse origin/main)` — the same operation, nothing reaches main, and the only form observed to pass.
- 2026-07-19 (M72): a milestone can violate the very lesson it CITES, in the prose it writes to record that lesson — M72 quoted M70 (never pin a count) and M71 (never generalize past the source), then shipped "the six off-shelf entries" and "exactly the complements — ICC(A,·)…", caught only by independent review (scored 92, 85). Citing a lesson is not applying it: at review, grep the milestone's OWN new prose for bare counts and for universals ("only", "every", "exactly", "all") and re-check each against its source.
- 2026-07-19 (M72): a source arriving MID-milestone stales every summary line already written — Brennan Ch. 3 arrived post-gate and closed one of two outstanding legs, leaving the `ORACLES.md` `Extraction:` header reading "Two legs … the single outstanding row": stale and self-contradictory in one sentence, an AC7 miss. After any late arrival or late fact change, re-read every header that SUMMARIZES a table, not just the table. The summary is where staleness hides, because the table looks right.
- 2026-07-21 (M75): for a BALANCED one-way random coverage sweep, the glmmTMB REML ICC(1) point equals `max(0, ANOVA-MoM rho)` exactly (σ²_a truncated at 0), so the sweep can validate coverage AND the reported point analytically — no live fits (M75 avoided ~14000 glmmTMB fits). Verify `icc()` reports exactly this point end-to-end in the fast unit suite, then run the heavy sweep fit-free against the shipped reducer in `data-raw/`. Corollary: an ICC(k) interval that is an exact monotone image of ICC(1) inherits coverage rep-by-rep — assert equality of the two coverage indicators (tolerance 0) rather than re-simulating.
- 2026-07-19 (M72): two `cairn_validate` traps for a references `Extraction:` status. (1) `references staleness` "contradicts itself" comes from a per-CLAUSE parse whose splitter merges across sentence boundaries, so a `not` in one sentence binds a later sentence's `verified`; the literal `unverified` always claims never, and `confirmed` is not a verify verb. State the status once, express exceptions without a competing verb, and locate a trip by running `_clause_claims` over the file. (2) `iso date format` is a hard FAIL that also scans WORK LOGS, so quoting a source's own non-ISO date fails the gate — paraphrase it; a disclosure quoting the offending string re-triggers the rule it discloses.
- 2026-07-21 (M77): `paths-ignore` on a `pull_request` trigger filters the PR's cumulative base…head diff, NOT the incremental push — a PR touching any non-ignored file (e.g. the workflow itself) re-runs the job on every later tracking-only sync commit; it saves work only on direct `push` events. Put it on `push` to skip docs-only commits to main; don't expect it to skip mid-PR tracking commits. To slim a matrix per-event: `config: ${{ github.event_name == 'push' && fromJSON('[...]') || fromJSON('[...]') }}` (actionlint-clean), and the PR's own run is the live self-test of which configs fire. **Correction (M78, 2026-07-21):** the "install dominates" claim is WRONG — `setup-r-dependencies@v2` already caches (install ~33–52s); CI wall-clock is the `check-r-package` **testthat suite** (ubuntu 13m/24m CPU, Windows 18m). Size CI-time estimates from the test step, not the install; the real lever is test-suite runtime (M78, D-011), not dep caching.
- 2026-07-21 (M76): authoring new `cairn/references/` pages adds candidates to M74's generalizing-claim completeness gate (`data-raw/enumerate-generalizing-claims.py --check`), wired into the CI `check-references` job by M81 — any range/superlative/"never"/"exactly one"/decimal-range claim on a new page needs a triage-ledger row or `--check` reds CI. When a milestone touches `references/`, triage the new claims via M74's categories: a repo synthesis note → `OUT-repo-analysis` (mirror `npbootstrap-oneway-comparison.md`), a verbatim source-attributed quote → `OUT-quote`, an `ORACLES.md` self-characterization → `OUT-oracle-pin`. Generate rows programmatically (the key is `citekey:sha1(norm(line))[:10]`) to avoid hash transcription errors.
- 2026-07-21 (M78): testthat's parallel worker count is HARD-CAPPED at 2 by default (`testthat:::default_num_cpus()` → `getOption("Ncpus")` → `TESTTHAT_CPUS` → 2); set `TESTTHAT_CPUS=$(getconf _NPROCESSORS_ONLN)` in the R-CMD-check job to use the whole runner. But the gain is runner-bound: GitHub ubuntu is ~2.5 usable cores (2→4 workers left ubuntu testthat elapsed flat at 13m, CPU 24m→33m), only Windows benefited (18m→15m) — size CI-parallelism from ~2–3 usable cores, not getconf's logical count. Reinforces the M77 paths-ignore/cancel line: a docs-only push to a PR branch CANCELS the in-flight R-CMD-check (cancel-in-progress + the PR cumulative diff defeats paths-ignore) — hold pushes while a measurement CI run is in flight.
- 2026-07-21 (M79): a registry/label-coverage checker must count as "covered" ONLY a `###` heading or a bolded `**…**` definition — a bare prose cross-reference masks a real gap (O-cluster-ck hid behind a passing mention until the rule tightened); and it must guard suffix-stripping so a standalone suffix-named oracle (O-sim, O-lme4) never collapses to a bare prefix token (`O`) that wildcard-matches everything (review finding F1, scored 82). Run the checker RED first to freeze the exact gap — a hand census undercounts (it missed O-LME vs the M1 cross-engine O-lme4, and O-FNML masked by another entry's prose).
- 2026-07-21 (M80): a `data-raw/` bookkeeping/ledger file that lists citekeys (e.g. `generalizing-claims-triage.tsv`) trips every D-009 source-note `! git grep -qiF '<citekey>' -- ... data-raw ...` "nothing references me" directive as a false positive. The faithful fix is a single-quoted `':(exclude)<ledger-path>'` pathspec appended to the directive (survives the checker's `bash -c`), NOT narrowing the scope to `data-raw/*.R`, which would blind the guard to a genuine future script reference; edit the adjacent prose enumeration in lockstep (D-009 rule 1). Any new data-raw bookkeeping file naming a citekey re-trips these directives — exclude it too.
- 2026-07-22 (M82): an ICC(k) "identity cross-check" that recovers the direct form by ALGEBRAICALLY INVERTING the package's own ICC(1) endpoint is tautological — `1 - 1/g_of_rho(ρ)` collapses to `npb_sb(ρ, n)`, so it only re-checks that the averaging divisor is n and a wrong ICC(1) core still passes (caught by the diff-bug lens, scored 80). Build the direct side from RAW statistics (SEARLE: the mcgraw Table 7 ICC(1,k) limits from the raw ANOVA F via the swapped-df identity `qf(p,a,b)=1/qf(1-p,b,a)`, as AC2 does for ICC(1)) and MUTATION-PROVE it diverges on a wrong df before trusting it. Where no independent construction exists (Burch's ICC(k) is inheritance-not-anchor, D-013), scope the test honestly to the SB identity and lean on the ICC(1) core's own published oracle. Corollary: a full local suite at `NOT_CRAN=true` (no `CI=true`) RUNS the live-brms `skip_on_ci` tests, so it can surface pre-existing rot in tests the CI/review gate never executes — re-run at `NOT_CRAN=true CI=true` for gate parity, and file the rot as out-of-scope rather than fixing it in-milestone.
- 2026-07-23 (M83): pinning a brms/engine fit to an explicit `type=` (or `level=`) in a test silently breaks that block's WHOLE-VECTOR `estimate == estimate` comparisons against paired glmmTMB/lme4/reference fits still at the default — the reference returns all four formulations (or both levels) while the pinned fit returns a subset, so lengths mismatch, or R recycles the shorter into the longer and `all()` passes by luck. Pin the paired references to the SAME `type=`/`level=` so the block compares one family end to end; blocks that extract via `by_index()`/single-row selection are length-robust and need no change. Cost a second full-Stan verify cycle (the index-assertion fix surfaced 3 downstream comparison failures the baseline hid behind the earlier failures).
- 2026-07-23 (M84): extending a variance-stabilized transformed bootstrap-t from balanced to unbalanced, studentize the pivot the IJ SE is DERIVED for (`theta = log SSA − log SSE`), NOT `log F` — they diverge under imbalance because a whole-subject resample changes total `N*` and hence the df-constant `C = log{(N−k)/(k−1)}` that separates them; on balanced data `N*` is fixed so `C` is invariant and they coincide (this is why the M75 balanced code got away with studentizing `log F` directly). The eq. 7 / Appendix-A IJ SE influence value already carries `n_i`, so the SE formula itself is the general form — only the pivot and the transform's group-size constant (`n0`, ohyama eq. 3, not the harmonic mean) need the unbalanced treatment.
- 2026-07-23 (M84, corrected 2026-08-03): a one-way CI reducer's SSE=0 (zero within-subject variance) degenerate guard is UNREACHABLE via `icc()` — the glmmTMB point-fit crashes first with a raw, non-`intraclass`-classed error, so an `expect_error(icc(...), class="intraclass_singular_fit")` on that input fails. Exercise the reducer's classed abort DIRECTLY (`npbootstrap_ci(groups, ...)`, `classical_guard_observed(ss, ...)`, or a stub `engine$simulate_refit` for `bootstrap_ci()`), reserving the `icc()`-path test for the SSA=0 boundary the engine tolerates. **Correction: "crashes first" is PLATFORM-DEPENDENT, not universal** — the 2026-08-03 abort-remedy hotfix wrote three `icc()`-level tests on exactly-zero-MSE data that passed locally (macOS/aarch64, the fit converges and the guard fires) and errored on both CI runners with `LU factorization of .gCMatrix failed: out of memory or near-singular`. A green local suite is NOT evidence that such a fixture reaches the guard; route it through the reducer regardless of what the local fit does. **(M103, extended 2026-08-05: the crash has more than one form — `NA/NaN gradient evaluation` on the ubuntu and windows runners where 2026-08-03 saw `LU factorization ... near-singular`, so match on the guard being unreached, never on an error string. And `check-standard.yaml` runs ONLY ubuntu + windows on PRs, macOS being push-to-main, so a macOS-only `skip_if` on such a fixture leaves its criterion with no PR-CI evidence at all.)**
- 2026-07-23 (M85): the `check-references` CI job runs the M74 generalizing-claim completeness gate (`python3 data-raw/enumerate-generalizing-claims.py --check`), which `cairn_validate` does NOT cover — so a locally-green consistency gate can still fail CI. Any edit to a `references/` page (ORACLES.md, INDEX.md, a `<citekey>.md`) that adds a generalizing claim — a "for every …", a coverage/width figure, a bare numeric range — needs a triage row in `data-raw/generalizing-claims-triage.tsv` keyed by the enumerator's `citekey:hash`; a repo-internal claim (a derived result or a committed fixture, not an external source table) triages `OUT-oracle-pin`. Run the enumerator locally before pushing a references-page edit. **(M91, extended 2026-07-25: that job runs on a PYTHON-only runner with no R, so a D-009 settling directive written as `check: Rscript -e ...` always reports FALSIFIED there — `Rscript: command not found` — while passing locally where R exists. Any `check:` directive must be settleable with python3/shell/git alone; if the claim is about `.rds` contents, restate it against committed TEXT (a markdown table, a script literal) and mutation-verify the command reds when the fact changes.)** **(M97, extended 2026-07-31: the job's THIRD checker, `data-raw/check-mpl-doc-claims.py` (M94), pins `@param conf_level`/`ci_method` and NEWS MPL-scope SENTENCES in `data-raw/mpl-doc-claims.tsv` — any rewording of those roxygen blocks needs its ledger row re-triaged (quote + claim key) and a new universal/negative sentence needs a new row; it bit twice in one M97 review. Run all three data-raw checkers locally before pushing an edit to that doc scope.)** **(M104, a THIRD recurrence 2026-08-05: two new `@param ci_method` sentences reddened `check-references` in CI having passed every local gate — `cairn_validate`, `devtools::check()`, lintr and air all green. A claim the ledger cannot settle (its fixture is M92 MPL cells) still needs a row, dispositioned `out` with a reason naming the instrument that does settle it.)**
- 2026-07-23 (M86): the M80 D-009 lesson generalizes past "bookkeeping" — ANY new `data-raw/` file whose comments name a citekey (e.g. a from-scratch implementation of a cited method that writes `xiao2013` in its header) trips that source note's `! git grep -qiF '<citekey>' -- ... data-raw ...` settling directives, reddening the `check-references` CI job via `check-reference-observations.py` (which `cairn_validate` does NOT run). When a milestone adds `data-raw/` code referencing a cited paper, in the SAME milestone update that `<citekey>.md`: append `':(exclude)data-raw/<file>'` to each directive AND fix the now-stale "nothing references this method" prose (if the code genuinely implements it, the claim is false, not just a false positive). Recurred at M86 review because it was not checked at implement time — run `python3 data-raw/check-reference-observations.py` before pushing any new data-raw file that names a citekey.
- 2026-07-23 (M86): estimating κ_m := max over a grid of an MC-noisy per-cell statistic by taking the empirical GRID MAX is upward-biased (winner's curse — max of noisy estimates), and the bias is worse for a noisier statistic (a 0.95 one-sided tail quantile vs a 0.90 two-sided one), so `(3,50)` one-sided overshot published 1.20 by ~0.13 while two-sided stayed within ±0.10. When the argmax location is known/identifiable (here the (ρ_L=0.6, δ_U=16) corner, since the correction grows toward small ρ / large δ), estimate κ_m by evaluating THAT cell directly — the unbiased estimator — not the grid max; and validate a calibration constant by its DEFINING property (the interval built at it covers at nominal) rather than by reproducing the noisy constant.
- 2026-07-24 (M88): calibrating a per-geometry constant like MPL's κ_m — the argmax over the (ρ,δ) grid is NOT fixed at the (min ρ, max δ) corner across geometries (M86/M87 established the corner only for R∈{3,5}; it moves interior at R=2 and R≥8), so a corner-direct evaluation under-estimates κ_m (→ narrower interval → under-coverage). Locate it per geometry via a full scan, then bias-correct by re-evaluating the top-k cells at high n_mc (max of them) — the argmax location is itself MC-noisy (winner's curse in the location, not just the value).
- 2026-07-27 (M93): `cli::cli_abort()` renders a message vector with `format_inline()` and lets rlang wrap it afterwards — NOT `cli::format_message()`. A test needing the text a user really sees must render with `format_message()` (which also strips cli's `\\` line-continuations; `format_inline()` leaves them in the string, so a naive comparison never matches) and then PIN that rendering by substring against the real abort, because the two calls are not identical and only the substring check establishes agreement.
- 2026-07-31 (M94): a stdlib-only Python RDS parser (~130 lines: gzip + XDR header; VECSXP/STRSXP/REALSXP/INTSXP/LGLSXP/CHARSXP, pairlist attributes, SYMSXP ref table, ALTREP compact/wrap) suffices to read a committed R fixture in an R-free CI job — no pyreadr/rdata pip dependency; verify it by exact-value comparison against `readRDS` on the real fixture and keep it fail-closed (raise on any unhandled SEXP type). Import it from another script via `sys.path.insert(0, 'data-raw')`.
- 2026-07-31 (M95): a `%.17g` decimal does NOT round-trip an R double through R's own parser — `R_strtod` lands 1 ulp off (31 of the 162 κ_m cells) — so a bit-exact text fixture must pin C99 hex floats (`sprintf("%a", x)`, re-read exactly by `as.numeric()`), keep a `%.17g` column only as a human-readable mirror, and assert the write→read round trip with `identical()`; measure any count stated in a header (`stopifnot`) rather than asserting it — the first draft's "10 of 162" was one slice's count. Related gotcha from the same harness: `on.exit()` never fires at Rscript top level; use `options(error = )` for mutate-and-restore cleanup.
- 2026-07-31 (M97): a direct-test harness for icc-internal helpers must build estimands/n0 with `summarize_design()`'s OWN harmonic-k_eff expression — the raw rater count verified a DIFFERENT estimand than production on unbalanced data (different SB pole and D-010 floor), silently inverting seed-level verdicts the acceptance evidence then pinned; and use the expression bit-identically (`1/mean(1/per_subject)`), since an algebraically-equal form (`n/sum(1/n_i)`) lands ulps off and reds a tolerance-0 endpoint comparison.
- 2026-08-01 (M98): testthat's default `testthat.progress.max_fails` (10) terminates a test run at the 10th failure — a mutation/removal measurement whose expected failure count can reach 10 must raise it (`options(testthat.progress.max_fails = 1000)`) or the measured count is censored at exactly 10, indistinguishable from a true 10.
- 2026-08-01 (M99): before claiming a defensive numeric branch "unreachable with real data" (and testing it only via a mock), probe the degenerate corners — perfect/near-perfect agreement, a variance component ~0. M99's plan-audited premise (f(rho_hat) = -crit < 0 always) was falsified at review by a perfect-agreement matrix: mpl_fit's joint Nelder-Mead minimum and the profile deviance disagreed by ~700 units, the "unreachable" abort fired via icc(), and pre-M99 code had been fabricating [0, 1] there. Two optimizers sharing a reference (joint fit vs profile) can diverge exactly where the likelihood degenerates, so guard the reference (check f at the MLE), don't assume it.
