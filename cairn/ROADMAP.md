# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-08-24 (M137 planned and added as an M48 dependency; the widened-claim and `cli`-prose candidate rows amended at its plan gate, the latter's file-count claim corrected. Two merDeriv rows compressed to hold the byte budget. `cairn_validate` exit 0.)_

Pre-migration history (M1–M47, ADR-001..058): see `cairn/legacy/` and git log.

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M48 | v0.1.0 release consolidation — CRAN submission-ready | blocked | M49, M50, M51, M53, M54, M55, M61, M68, M129, M130, M131, M132, M133, M134, M135, M136, M137 | high | milestones/M48-release-v010.md |
| M137 | R6 claim audit over the M134-M136 prose diffs | review | — | high | milestones/M137-r6-claim-audit.md |
| M134 | Vignette prose pass — the reader path, and the house style standard | done | — | normal | milestones/archive/M134-vignette-prose-reader-path.md |
| M135 | Vignette prose pass — the method articles | done | M134 | normal | milestones/archive/M135-vignette-prose-method-articles.md |
| M136 | Roxygen and README prose pass | done | M134 | normal | milestones/archive/M136-roxygen-readme-prose.md |
| M132 | Make the two prose-only `icc()` argument values reachable from the docs | done | — | normal | milestones/archive/M132-prose-only-icc-arguments.md |
| M133 | Tell users which interval method is trustworthy for their design | done | M130 | high | milestones/archive/M133-ci-method-trustworthiness-table.md |
<!-- terminal-row retention: M136 done (2026-08-24) → M131 rotated out (oldest terminal by merge order, PR #140; its archive file still resolves there). Kept: M132, M133, M134, M135, M136 (5 most recent terminal) [claim:roadmap-terminal-rows]. -->
<!-- rows grouped by status; keep only the 5 most recent terminal (done/dropped)
     rows — older history in cairn/legacy/ + git. -->


## Candidates

- **A mechanical check that a prose pass never widens a claim** — M136's descoped AC3: a mechanical predicate over scope-carrying constructions, replacing the reading M136 found non-convergent (three review rounds, each closure defeated by a fresh shape — an em dash, a parenthesis, a bare frame adverbial). M137 takes the *audit* over the same hunks and refuses the *checker*: D-021 bars a new guard over this repo's own doc claims absent a defect in what the package computes, and D-029's M116 precedent is on point. Promote on a mechanical predicate, or on a user reaching a widened claim — never on the count of instances; the second clause supersedes D-021 for this scope. Lineage: M136 returns 1–3 → descope 2026-08-24 → M137 plan gate 2026-08-24.
- **Harden the prose ruler; point prose criteria at the prose** — `data-raw/prose-profile.py`'s six blind spots are listed in `cairn/doctrine/prose-style.md` (a `---` break, a setext underline, a pandoc simple/grid separator row, a joined bullet list, comments stripped before fences, a pipe-less row not split per cell); `RE_DASH` also counts each occurrence in a dash run. None occurs in `vignettes/`. The audits found M134's AC1 binding the ruler not the prose, AC3 binding two records to each other. Deferred at M134's amendment gate and its fourth-review gate (2026-08-23): a ruler change restarts the frozen baseline. Promote on a pass hitting one of the shapes, never on the count. Lineage: M134 amendment gate; fourth-pass [O] 1, 2, 7.
- **`cli` abort and hint strings get the M134 prose pass** — the `cli` condition surface is 135 `cli_abort`/`cli_warn`/`cli_inform` sites across 14 files, 45 of them in `R/icc.R` (corrected M137 — this row previously named only `R/abort.R` and `R/boundary-hint.R`, of which the former is the condition layer, not user text); it is the same D-029 class as the vignettes and roxygen, but was fenced out at the M134–M136 plan gate (2026-08-23): they are guarded by `data-raw/check-abort-remedy-verdicts.R` and pinned by rendered-message tests (M93's `format_message()` substring pins, M127's withdrawn-wording pin), so a wording pass re-keys guards a documentation pass never touches. Promote on a user reporting an abort message as hard to read, or on the next milestone that edits abort text for another reason — never on the dash count alone — both files already measure zero dashes (M137 plan gate). Lineage: M134–M136 plan gate, criteria-audit note 2; M137 plan gate 2026-08-24.
- **The `school` fixture is rebuilt verbatim five times in `test-vignette-claims.R`** — the seeded 16x5x4 simulation the multilevel article builds at `vignettes/multilevel-designs.Rmd:45-66` is rebuilt inline at `tests/testthat/test-vignette-claims.R:409`, `:477`, `:570`, `:663` and, since M132, `:731`, with nothing tying any copy to the vignette chunk: a change to the article's simulation needs five test edits and reds nothing if it gets one. Four copies predate M132 and the file's other fixtures follow the same convention, so this is the file's idiom rather than a defect M132 introduced. Promote on a vignette simulation change leaving a copy stale, or on a sixth copy being wanted — never on the count of copies. Lineage: M132 review [O] finding 11.
- **A per-`ci_method` trustworthiness table for `interval-methods.Rmd`** — M133's reverted deliverable: per `ci_method`, the family computed, the family refused, the depth verified. Not barred by D-021 (D-029: user docs plan normally). It failed because its cells were COMPOSED from a reading of the fences; two rounds each found new cells false. Promote on a way to DERIVE cells from code. Prose: git, PR #142. Lineage: M133 returns 1–2 → descope 2026-08-23.
- **A standing test that such a table matches the shipped surface** — the guard making the row above safe. Refused at the M133 plan gate, unchanged by the descope: such a guard needs D-021's trigger and D-029 retains the bar (`DECISIONS.md:1300`). M133's pins are not it: they pin exported abort behaviour, D-021's carve-out; gaps logged in that archive. Promote on a user reaching a misdescribed cell, never without superseding D-021 first. Lineage: M133 descope 2026-08-23.
- **A `ci_method = "bootstrap"` caller is made to install `merDeriv`, and does use it** — every dispatched `fit_lme4*()` checks for `merDeriv` as its second unconditional statement, before any interval-method branching, then forms the SD-scale parameter covariance eagerly (`git grep -n 'merDeriv::' -- R/engine-lme4.R`: 7 unconditional `vcov.lmerMod()` sites; a traced bootstrap run calls it once, measured 2026-08-18 on `ratings`, `boot_samples = 5`, `seed = 1`, no `data-raw/` script). Sparing a bootstrap caller the dependency therefore means making the covariance conditional on the interval method — a change to the engine contract each `fit_lme4*()` returns. M16 kept the check unconditional deliberately (`033501b2`, `9ebf5ad4`). Promote on a user reporting the install as unnecessary for a bootstrap run, never on the redundancy alone. Lineage: M127 review F10 → return-round [O] lens F1, which falsified this row's first premise before it shipped.
- **No standing guard that a new lme4 entry point checks for `merDeriv`** — M127 corrected the requirement message and audited the code behind it (12 dispatched `fit_lme4*` functions, each carrying the check as its second unconditional statement; no `lmer`/`bootMer` call outside `R/engine-lme4.R`), but that audit is a SNAPSHOT: nothing tests it. A 13th dispatched entry point skipping the check would leave both the shipped message and the source comment at `R/engine-lme4.R:50-58` silently false, and M127's pin is blind to it — it reds only the WITHDRAWN wording, never a true claim gone stale. Deferred at the M127 implement gate rather than widened mid-implementation: a structural guard over this repo's own source is apparatus under D-021 pressure and wants its own plan gate. Promote on a new lme4 entry point being added, or on a user reaching an lme4 fit that does not check — never on the count of entry points. Lineage: M127 AC4 → [O] reader finding 12 → implement gate 2026-08-18.
- **The checkpoint routing check's known blind spots** — descoped at M120's fourth review rather than attempt a fifth universal reachability rule; all four (a)–(d) are declared in `data-raw/checkpoint-sites.tsv`, promised by no criterion, full text in git. The sharpest: a future fixture-digest spec entry must be a **character scalar** declared in position, because `ckpt_spec_mismatch()` (`data-raw/checkpoint-guard.R:297`) compares numerics at 1.5e-8 relative tolerance, too loose for the 1e-9 edit a digest catches (`check-checkpoint-sites.R` rule 5; migrated from the M122-promoted row, M125). Narrative: `milestones/archive/M120-checkpoint-staleness-guard.md`, `milestones/archive/M122-drop-m121-resume-cache.md`. Promote on a declared site needing one of these shapes, or on a reversion reaching a fixture through one — never on the count of shapes. Lineage: M120 return 4 → the return-4 descope (2026-08-15) → M122 plan-gate audit (migrated M125).
- **Deserialization forms the checkpoint watcher never sees** — the run-time watcher traces `readRDS` only, verified at the M120 re-cut audit by running them: `load()` and `unserialize(readBin(...))` of a registered checkpoint both return silently and are absent from the end-of-run assertion, as is any `readRDS` given a connection. M120 fences this rather than closing it: its AC2 plants a call to each name in a declared list (`readRDS`, `load`, `unserialize`, `readr::read_rds`) in each declared site and requires the static check to report it, so a site ADOPTING one of those forms reds, while the watcher still cannot observe one at run time. Promote on a declared site needing to read its checkpoint by any form the watcher does not trace — that is the falsifier, not a count of forms — or on a sixth harness arriving that resumes through one. Lineage: M120 re-cut criteria audit round 2.
- **Extend the M113 skew battery to `ci_method = "bootstrap"`** (corrected M121) — of the four methods originally called unrun, only `"bootstrap"` remains: `"mpl"` and `"posterior"` abort on the one-way design by their own fences — measured at the M121 plan gate: `"mpl"` aborts classed `intraclass_unsupported`, `"posterior"` aborts requiring `engine = "brms"` (full pre-M125 text in git) — the two-way case has its own row below, and `"npbootstrap"` ran as M121 (`milestones/archive/M121-npbootstrap-skew-coverage.md`). `"bootstrap"` runs at 10.6–15.0 s per call — ~114 h for the full 64-cell × 2000-rep grid even at 4 workers — which is why M121 fenced it out; M115's caveat stays bounded to `mc`/`searle`/`burch` until this lands, and D-027's S1 reopening clause still applies. Promote on a reduced-grid design that keeps the frozen floor comparable, or on a per-call cost bringing the full grid under a working day. Lineage: D-027 → M115 plan gate → M121 plan gate.
- **A two-way heavy-tail grid, so the agreement-only methods can be measured at all** — `ci_method = "mpl"` is fenced to the two-way random absolute-agreement ICC(A,1)/ICC(A,k) with one rating per subject, and `"posterior"` needs the brms engine; both therefore refuse every cell of the one-way M113/M121 grid by design rather than by cost, so nothing this repo has measured says anything about their behaviour under skew or heavy tails. Closing that needs a second grid: a two-way random DGP with the non-Gaussian family applied per burch2011 §3 as M118 did, its own published anchor, its own frozen rules. Substantially larger than M121: a new generator, a new anchor, and for `"posterior"` a Stan fit per replicate — costlier per call than the `"bootstrap"` leg fenced out above. Promote on a need to advise users about the two-way methods under heavy tails, or on an anchor being found that makes the grid cheap to validate. Lineage: M121 plan gate (2026-08-15).
- **Cluster-effect-direct trigger statistic family** — the reopening path D-028 names: the frozen kurtosis/skewness family failed by dilution (the pooled z carries the cluster effect at weight² ≈ 0.04 at n = 5), so a second frozen assessment would measure the cluster effect directly (moments of subject means / predicted effects, standardized within-design). Promote on a pilot showing such a statistic separates the failing cells at the D-028 floors — the evidence class that falsifies the `document` choice — never on re-thresholding the M114 family. Lineage: M114 (D-028).
- Burch's near-zero-MSA interval is **computed from floating-point roundoff** — `burch_kappa_hat()` standardizes by `sqrt(MSA)`, so at an MSA that is tiny but not exactly 0 both numerator and denominator are accumulated summation error. Measured at the M105 review (2026-08-05) on the committed 8×4 fixture cell: MSA = 3.52e-33, every subject-mean deviation exactly −2.78e-17 against `sqrt(MSA)` = 5.93e-17, ratio −0.468, `kappa_hat` = −1.7395, interval [−0.1865, 0.2974] at `unit = "single"`. **Pre-existing, not introduced by M105** — the same cell returned the same interval before it — which is why M105's guard (`identical(MSA, 0)`) leaves it alone and M105 AC3 certifies it as a valid finite result. Promote on a user reporting such an interval, or when the M105 re-cut decides the certification should not stand. Lineage: M105 review F1.
- Harden the **M104 containment sweep and its fixture guards** — three latent risks its review logged below the action bar, none affecting what shipped. (a) The fixture test's two `all()` assertions are vacuously true on zero rows, and its anti-vacuity guard checks the total row count where AC1's requirement is per-arm, so a skewed regeneration passes (review F3/F4). (b) `identical(cell$arm, "zero-between")` in `data-raw/sweep-bootstrap-point-containment.R` is a factor comparison under the declared `R (>= 3.5)` floor, where a re-run would route every cell to the wrong generator and label it correctly anyway (review F8). (c) The fixture's `seed` column is the data seed while the reducer seed and engine live only in the prose header (F11). Promote on the next regeneration of that fixture or the next change to that script — the guards bite only under a future edit. Lineage: M104 review.
- **Deferred remainder of the M104 bootstrap point/interval work** — three pieces fenced out at the M104 plan gate (2026-08-05), each promoted only on evidence M104's own measurement does not already answer. (a) Reconciling the reported point to its interval (clamping `conf.low` down to the estimate) — promote on a measured excluding cell whose gap falls outside M104 AC2's `1e-8` bound, or whose point estimate is materially nonzero; the 42-cell plan-gate sweep found neither. (b) A containment condition in `boundary_interval_usable()` — rejected because it would silence M103's hint on exactly the data M103 exists to serve; promote if that acceptance rule is revisited for another reason. (c) Extending the containment sweep to the other five reducers — promote on the same shape being reported against another `ci_method`. Lineage: M93 hint → M103 AC6 → M104.
- Two degenerate-data behaviors **deliberately kept** at the M105 plan gate (2026-08-05), each recorded in place at `R/ci-npbootstrap.R:126` as correct rather than defective. (a) `ci_method = "searle"` at `unit = "average"` on zero-between-subject-variance data reports `conf.low = conf.high = -Inf` — the correct limit of the Spearman-Brown projection, and in-support for the projected form under D-010's `(-Inf, 1)`; whether a zero-width interval at `-Inf` is a thing to hand a user is a separate question this milestone did not open. (b) `npb_guard_sb_pole()` refuses endpoints strictly past the pole (`denom < 0`) but tolerates the pole itself (`denom == 0`), pinned by `tests/testthat/test-boundary-abort-hint.R:2240`. Reopening either supersedes D-010 and reaches all three `ci_method` values sharing that guard, so promote on a user reporting one of them as a defect, not on the shape alone. Lineage: M100 sweep → M105 plan gate.
- MPL κ_m **monotone envelope / smoother** over the shipped table's S dips — the calibrated κ_m is non-monotone in S at every level (worst downward step −0.046 at 0.90, −0.068 at the shipped 0.95, −0.162 at 0.99 [claim:kappa-worst-steps]; MC noise, bootstrap SE ≈0.03–0.04), so linear-in-S interpolation is not conservatively biased as `R/ci-mpl.R` once claimed; **chosen against at the M91 plan gate (2026-07-24)** for traceability. Every probe since cleared its frozen floor — M91's three off-node cells (D1–D3, incl. R=2 at 0.99 across the −0.154 dip) and M92's three 0.95 off-node cells, including one across 0.95's worst dip — so the pre-registered bracket-max consequence never fired and the lookup is unchanged. Promote on a cell that falls below its floor, never on the non-monotonicity itself; per-cell figures in § M92 of `cairn/references/mpl-twoway-random-comparison.md`. Lineage: M88 table → M90 tables → M91 plan gate → M91 T2 → M92 T7.
- Restore **high-side (`pmin(1, .)`) clamp detection** in the AC4 endpoint-parity test (`test-boundary-abort-hint.R`) — low-side detection survived the 2026-08-01 pole hotfix (measured at the M98 plan gate; supplier cells per class in `milestones/archive/M98-clamp-detection-assertion.md` and this row's full pre-M125 text, git), but no shipped `ci_method` can report an endpoint above +1 since `npb_guard_sb_pole()`, and the synthetic-reducer probes were rejected at that gate because they bypass the reporting path the test exists to pin. Promote on a shipped path that can again report above +1, or on a probe design that keeps the reporting path in the loop. Lineage: M93 AC4 → hotfix `hotfix-searle-pole-out-of-support` (PR #105) → M98.
- Ledger + CI checker pinning **abort-remedy truthfulness** — chosen at the M100 plan gate over a follow-on milestone; kept separate from M102 at its plan gate (the two share a row schema and checker idiom — build on M102's rather than design a second); **barred by D-021 (2026-08-03), not dropped**. Promote on a remedy bullet measurably misleading a user — a defect in what the package computes — never on the drift alone. Narrative: `milestones/archive/M100-abort-remedy-truthfulness.md`, `milestones/archive/M102-record-claims-ledger.md`. Lineage: M93 T1 → M100 → M102 plan gate → D-021.
- Harden `data-raw/check-mpl-doc-claims.py`'s recall net and self-test — M94's sub-80 findings (a trigger vocabulary missing the comparative claim class, a hard-coded absent-row probe, brittle scope anchors, structural settle assertions), plus two measured instances since: M106 F9's un-enumerated "any other design" fence claim, and the PR #115 scope gap — `news_scope` sweeps only the M94 bullet, so the M88-era MPL bullet's falsified "finite interval on every dataset" sentence shipped stale; extend the NEWS scope when hardening. None affects what shipped; each bites under future edits. Narrative: this row's full pre-M125 text (git; the M94/M106 archives record the carry, not the defects). Promote via `/milestone-plan` on the next MPL doc-surface change — a condition that fired once and was re-deferred at the M106 plan gate, which extended the checker's scope only — or fold into the M95 pin work. Lineage: M94 review O-10/O-14/O-15/O-16/O-19–O-22 → M106 plan gate → PR #115.
- **Per-class reachability proof for the doc-claim pin** — split off M123 at its sizing gate and refused a milestone at the plan gate (2026-08-16): its deliverable is a guard over the repo's own records, which D-021 bars and D-029 declines to exempt. The eight deferred pieces (a)–(h) are named in `milestones/archive/M123-falsified-capability-claims.md`; measurements, and the two mutation-harness gaps that leave a pin named outside the list silently never planted, are in this row's earlier text (git). Promote on a withdrawn claim returning to a shipped surface while the pin stays green, never on a count of coverage gaps, and never without superseding D-021 first. Distinct from the `check-mpl-doc-claims.py` hardening row above. Lineage: M123 review return 2 → sizing gate → plan gate 2026-08-16 → descope gate 2026-08-17.
- On-the-fly κ_m calibration for `ci_method = "mpl"` — the alternative to M88's precomputed table (D-015): calibrate κ_m at call time for the exact (R,S) geometry, exact but ~minutes and stochastic (needs a `seed`). Promote only on a concrete need for off-grid geometries the M88 table+interpolation doesn't serve. Lineage: D-014 (ii) → D-015 → M88.
- Companion software/methods paper (JOSS or similar) — after the v0.1.0 release; the M42 comparison article is the seed; venue/framing decided against the released package (design interview + plan gate, 2026-07-12; ADR-022 deferral) — cairn/DESIGN.md § Commitments
- Statistical-extension parking lot (grouped; see `cairn/legacy/ROADMAP.md` for the full descriptions + readiness/status per item): the `d_study()` cluster-level / occasion-ragged projection; the occasion-averaged coefficient on ragged replicates (research); incomplete/unbalanced **fixed** cluster-level `ICC(c,k)` (still blocked by ten Hove's small-`k` estimator). Promote individually via `/milestone-plan` — migrated 2026-07-12 — cairn/legacy/ROADMAP.md
- Incomplete/unbalanced fixed-rater **subject**-level multilevel lavaan — the SEM sibling that compounds two-level FIML with the Case-3A fixed correction; low priority, promote only on a concrete need. Split off from the lavaan-multilevel-siblings candidate at the M56–M58 plan gate (2026-07-17); the fixed **cluster** level stays double-blocked (parking-lot candidate above). Lineage: ADR-027 → M53 GO (D-005) → M54 → M56–M58
- Plotting: exported user-facing `theme_intraclass()` — a composable, exported ggplot2 theme users can add to their own plots. Deferred at the M61 plan gate (2026-07-17) in favor of internal-only styling; promote on a concrete request. Lineage: M61.
- Plotting: new view types beyond the three current views (e.g. a stacked variance-share / proportion chart). Deferred at the M61 plan gate (2026-07-17); multilevel views are already level-faceted, so a genuinely new display needs a concrete proposal. Lineage: M61.
- Multilevel lavaan bootstrap CI beyond balanced/complete random — two parked cells: (a) the crossed **fixed** cell (thread the per-refit Case-3A θ²_r correction through the M56 factory `lavaan_ml_simulate_refit`/`lavaan_multilevel_components`, currently random-only); (b) the **random incomplete/unbalanced** cells (the factory takes an unequal `cluster_sizes` vector, but coverage is validated only on balanced data — needs an unbalanced coverage oracle; incomplete can't bootstrap at all, ADR-031). Both MC-only ship; bootstrap parity is nice-to-have and inherits M56's cluster-level cross-platform flake. Lineage: M56 factory → M57 (fixed) → M58/MD-1 (random incomplete/unbalanced).
- lavaan + within-cell replicates — the SEM engine on replicated (σ²_sr/σ²_e-split) data. Niche, low value: would need both a lavaan replicate parameterization and the M20 replicate machinery to intersect. Promote only if a concrete need appears. Reclassified from M21 (ADR-027); promoted from the parking lot 2026-07-13 — cairn/legacy/ROADMAP.md
- d_study() CI-width precision planning ("how many subjects for a ±.1-wide interval?") — scope boundary resolved by the design interview (2026-07-12): a legitimate future direction, **gated on finding an oracle strategy**; subject-count-for-power as such stays out of scope (`M4.5-d-study.md` §6; DESIGN.md contract boundary) — cairn/estimand-specs/M4.5-d-study.md
