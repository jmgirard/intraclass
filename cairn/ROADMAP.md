# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-08-25 (M48 added four candidate rows — the deferred chooser, the untested R floor with GP3's stale cross-product, the `tidy()`/`glance()` doc gap, the spelling guard that cannot red — which took the file past the byte budget; the widest rows were compressed by deletion and the two M133 rows clustered into one to hold it. `cairn_validate` fails no check.)_

Pre-migration history (M1–M47, ADR-001..058): see `cairn/legacy/` and git log.

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M48 | v0.1.0 release consolidation — CRAN submission-ready | in-progress | M49, M50, M51, M53, M54, M55, M61, M68, M129, M130, M131, M132, M133, M134, M135, M136, M137 | high | milestones/M48-release-v010.md |
| M134 | Vignette prose pass — the reader path, and the house style standard | done | — | normal | milestones/archive/M134-vignette-prose-reader-path.md |
| M135 | Vignette prose pass — the method articles | done | M134 | normal | milestones/archive/M135-vignette-prose-method-articles.md |
| M136 | Roxygen and README prose pass | done | M134 | normal | milestones/archive/M136-roxygen-readme-prose.md |
| M133 | Tell users which interval method is trustworthy for their design | done | M130 | high | milestones/archive/M133-ci-method-trustworthiness-table.md |
| M137 | R6 claim audit over the M134-M136 prose diffs | done | — | high | milestones/archive/M137-r6-claim-audit.md |
<!-- terminal-row retention: M137 done (2026-08-25) → M132 rotated out (oldest terminal by merge order, PR #141; its archive file still resolves there). Kept: M134, M135, M136, M133, M137 (5 most recent terminal) [claim:roadmap-terminal-rows]. -->
<!-- rows grouped by status; keep only the 5 most recent terminal (done/dropped)
     rows — older history in cairn/legacy/ + git. -->


## Candidates

- **Nothing tests the declared `R (>= 4.0.0)` floor** — inferred from the Imports chain (rlang's `Depends`), never run: CI's oldest cell is `oldrel-1` and neither win-builder nor R-hub reaches 4.0.0, so GP3's "no declared floor CI doesn't test" is unsatisfied at v0.1.0. Same principle, second inaccuracy: GP3's text (`DESIGN.md:157-159`) reads "release/oldrel-1/devel x macOS/Windows/Ubuntu" where devel and oldrel-1 run on Ubuntu only. Both accepted at the M48 review gates as release-round work (a pinned CI job; a principle edit, which makes `cairn_impact.py --changed` owed). Promote on a user reporting a failure between 4.0.0 and `oldrel-1`, or on the next release round. Lineage: M48 review criteria audit 3; second pass [O] G4 (2026-08-25).
- **`tidy()$occasions` and `glance()$n_o` disagree numerically under near-identical names** — on one 8x3 replicate fit with two ratings per cell, `glance()$n_o` is 2 and every row of `tidy()$occasions` is 1: the first is the design's per-cell count, the second the estimand's averaging divisor (`R/estimand.R:56-61`). Both columns are new at v0.1.0 and neither doc says what the other means, so the pair reads as one fact contradicting itself. A doc gap, not a computation error, hence triaged out of the release round. Promote on a user reading the two columns as one quantity, never on the naming alone. Lineage: M48 review second pass, [O] G5 (2026-08-25).
- **`choose_icc()` accepts `type = "both"`** — the chooser's agreement-versus-consistency question takes one value while `icc()`'s `type` defaults to reporting both, so a user wanting the pair has no way to say so and gets a single-coefficient recommendation. Additive after release (a new accepted value, no existing call changed), hence triaged out of M48. Promote on a user asking the chooser for both definitions, never on the asymmetry alone. Lineage: RR04 recommendation 8 → M48 ingest 2026-08-25.
- **The spelling guard cannot red** — `tests/spelling.R` calls `spell_check_test(error = FALSE, skip_on_cran = TRUE)`, so neither `devtools::test()` nor an `--as-cran` run (which sets `NOT_CRAN=false`) can fail on an unlisted word; only a manual `spelling::spell_check_package()` catches one. Measured at M48's second review return, where a word the T7-T11 repair commit introduced failed AC3 after the first pass had measured spelling clean. Flipping `error = TRUE` is a one-word change, left out of the release round as D-021-adjacent apparatus over this repo's own prose. Promote on a second unlisted word reaching a review, or on the next release round. Lineage: M48 review second pass, [O] G1 (2026-08-25).
- **A mechanical check that a prose pass never widens a claim** — M136's descoped AC3: a mechanical predicate over scope-carrying constructions, replacing the reading M136 found non-convergent (three review rounds, each closure defeated by a fresh shape). M137 takes the *audit* over the same hunks and refuses the *checker*: D-021 bars a new guard over this repo's own doc claims absent a defect in what the package computes, D-029's M116 precedent being on point. Promote on a mechanical predicate, or on a user reaching a widened claim — never on the count of instances; the second clause supersedes D-021 for this scope. Lineage: M136 returns 1–3 → descope → M137 plan gate 2026-08-24.
- **Three doctrine modules state no budget in their own header** — `data-raw-checkers.md` (68 lines / 4,180 bytes), `doc-claim-pins.md` (89 / 5,138), `source-ingestion.md` (39 / 2,185) each graduated from LESSONS without the budget the maturation exit requires, so a hygiene pass has nothing to check them against; only `prose-style.md` carries one (< 120 / < 8,000). Measured 2026-08-25. Promote on a module needing a compression decision, or on the next graduation — never on the omission alone, all three sitting under their sibling's figures. Lineage: M137 hygiene.
- **Harden the prose ruler; point prose criteria at the prose** — `data-raw/prose-profile.py`'s six blind spots are listed in `cairn/doctrine/prose-style.md`; `RE_DASH` also counts each occurrence in a dash run. None occurs in `vignettes/`. The audits found M134's AC1 binding the ruler not the prose, AC3 binding two records to each other. Deferred at M134's amendment gate and its fourth-review gate (2026-08-23): a ruler change restarts the frozen baseline. Promote on a pass hitting one of the shapes, never on the count. Lineage: M134 amendment gate; fourth-pass [O] 1, 2, 7.
- **`cli` abort and hint strings get the M134 prose pass** — 135 `cli_abort`/`cli_warn`/`cli_inform` sites across 14 files, 45 of them in `R/icc.R` (corrected M137). Same D-029 class as the vignettes and roxygen, fenced out at the M134–M136 plan gate (2026-08-23): the surface is guarded by `data-raw/check-abort-remedy-verdicts.R` and pinned by rendered-message tests, so a wording pass re-keys guards a documentation pass never touches. Earlier text in git. Promote on a user reporting an abort message as hard to read, or on the next milestone that edits abort text for another reason — never on the dash count alone. Lineage: M134–M136 plan gate, criteria-audit note 2; M137 plan gate 2026-08-24.
- **The `school` fixture is rebuilt verbatim five times in `test-vignette-claims.R`** — the seeded 16x5x4 simulation the multilevel article builds at `vignettes/multilevel-designs.Rmd:45-66` is rebuilt inline at `tests/testthat/test-vignette-claims.R:409`, `:477`, `:570`, `:663` and, since M132, `:731`, with nothing tying any copy to the vignette chunk: a change to the article's simulation needs five test edits and reds nothing if it gets one. Four copies predate M132 and the file's other fixtures follow the same convention, so this is the file's idiom, not a defect M132 introduced. Promote on a vignette simulation change leaving a copy stale, or on a sixth copy being wanted — never on the count. Lineage: M132 review [O] 11.
- **A per-`ci_method` trustworthiness table for `interval-methods.Rmd`, and a standing test that it matches the shipped surface** — M133's reverted deliverable: per `ci_method`, the family computed, the family refused, the depth verified. It failed because its cells were COMPOSED from a reading of the fences, two rounds each finding new cells false. The guard that would make it safe was refused at the M133 plan gate and stays refused: D-029 retains D-021's bar (`DECISIONS.md:1300`). Promote the table on a way to DERIVE cells from code, the guard on a user reaching a misdescribed cell — never without superseding D-021 first. Prose: git, PR #142. Lineage: M133 returns 1–2 → descope 2026-08-23.
- **A `ci_method = "bootstrap"` caller is made to install `merDeriv`, and does use it** — every dispatched `fit_lme4*()` checks for `merDeriv` second and unconditionally, before any interval-method branching, then forms the SD-scale parameter covariance eagerly (7 unconditional `vcov.lmerMod()` sites in `R/engine-lme4.R`; a traced bootstrap run calls it once, measured 2026-08-18). Sparing a bootstrap caller the dependency therefore means making the covariance conditional on the interval method — a change to the engine contract each `fit_lme4*()` returns. M16 kept the check unconditional deliberately (`033501b2`, `9ebf5ad4`). Promote on a user reporting the install as unnecessary for a bootstrap run, never on the redundancy alone. Lineage: M127 review F10 → [O] F1.
- **No standing guard that a new lme4 entry point checks for `merDeriv`** — M127's audit (12 dispatched `fit_lme4*` functions each carrying the check second and unconditionally; no `lmer`/`bootMer` call outside `R/engine-lme4.R`) is a SNAPSHOT nothing tests. A 13th entry point skipping it would leave the shipped message and `R/engine-lme4.R:50-58` silently false, and M127's pin reds only the WITHDRAWN wording. Deferred at the M127 implement gate: a structural guard over this repo's own source is apparatus under D-021 pressure. Promote on a new lme4 entry point, or on a user reaching an lme4 fit that does not check — never on the count. Lineage: M127 AC4 → [O] 12 → implement gate 2026-08-18.
- **The checkpoint routing check's known blind spots** — descoped at M120's fourth review rather than attempt a fifth universal reachability rule; all four (a)–(d) are declared in `data-raw/checkpoint-sites.tsv`, promised by no criterion, full text in git. The sharpest: a future fixture-digest spec entry must be a **character scalar** declared in position, because `ckpt_spec_mismatch()` (`data-raw/checkpoint-guard.R:297`) compares numerics at 1.5e-8 relative tolerance, too loose for the 1e-9 edit a digest catches. Narrative: the M120 and M122 archives. Promote on a declared site needing one of these shapes, or on a reversion reaching a fixture through one — never on the count. Lineage: M120 return 4 → descope (2026-08-15) → M122 plan-gate audit (migrated M125).
- **Deserialization forms the checkpoint watcher never sees** — the run-time watcher traces `readRDS` only, verified at the M120 re-cut audit by running them: `load()`, `unserialize(readBin(...))` and any `readRDS` given a connection all return silently. M120 fences rather than closes this: AC2 plants a call to each name in a declared list in each declared site and requires the static check to report it, so a site ADOPTING one reds while the watcher still cannot observe one at run time. Promote on a declared site needing a form the watcher does not trace — the falsifier, not a count of forms — or on a sixth harness resuming through one. Lineage: M120 re-cut audit round 2.
- **Extend the M113 skew battery to `ci_method = "bootstrap"`** (corrected M121) — of the four methods once called unrun, only `"bootstrap"` remains: `"mpl"` and `"posterior"` abort on the one-way design by their own fences (measured at the M121 plan gate; text in git), the two-way case has its own row below, and `"npbootstrap"` ran as M121. `"bootstrap"` costs 10.6–15.0 s per call — ~114 h for the full 64-cell × 2000-rep grid at 4 workers — which is why M121 fenced it out; M115's caveat stays bounded to `mc`/`searle`/`burch` until it lands, and D-027's S1 reopening clause applies. Promote on a reduced-grid design keeping the frozen floor comparable, or on a per-call cost bringing the grid under a working day. Lineage: D-027 → M115 → M121 plan gate.
- **A two-way heavy-tail grid, so the agreement-only methods can be measured at all** — `ci_method = "mpl"` is fenced to the two-way random absolute-agreement ICC(A,1)/ICC(A,k) with one rating per subject and `"posterior"` needs the brms engine, so both refuse every cell of the one-way M113/M121 grid by design rather than by cost: nothing this repo has measured says anything about their behaviour under skew or heavy tails. Closing it needs a second grid — a two-way random DGP per burch2011 §3, its own anchor and frozen rules, and for `"posterior"` a Stan fit per replicate, costlier than the `"bootstrap"` leg fenced out above. Promote on a need to advise users about the two-way methods under heavy tails, or on an anchor making the grid cheap. Lineage: M121 plan gate (2026-08-15).
- **Cluster-effect-direct trigger statistic family** — the reopening path D-028 names: the frozen kurtosis/skewness family failed by dilution (the pooled z carries the cluster effect at weight² ≈ 0.04 at n = 5), so a second frozen assessment would measure the cluster effect directly (moments of subject means / predicted effects, standardized within-design). Promote on a pilot showing such a statistic separates the failing cells at the D-028 floors — the evidence class that falsifies the `document` choice — never on re-thresholding the M114 family. Lineage: M114 (D-028).
- Burch's near-zero-MSA interval is **computed from floating-point roundoff** — `burch_kappa_hat()` standardizes by `sqrt(MSA)`, so at an MSA tiny but not exactly 0 both numerator and denominator are accumulated summation error. Measured at the M105 review (2026-08-05) on the committed 8×4 fixture cell: MSA = 3.52e-33, `kappa_hat` = −1.7395, interval [−0.1865, 0.2974] at `unit = "single"`. **Pre-existing, not introduced by M105** — the same cell returned the same interval before it — which is why M105's guard (`identical(MSA, 0)`) leaves it alone and M105 AC3 certifies it as a valid finite result. Promote on a user reporting such an interval, or when the M105 re-cut decides that certification should not stand. Lineage: M105 review F1.
- Harden the **M104 containment sweep and its fixture guards** — two latent risks its review logged below the action bar, neither affecting what shipped. (a) The fixture test's two `all()` assertions are vacuously true on zero rows, and its anti-vacuity guard checks the total row count where AC1's requirement is per-arm, so a skewed regeneration passes (F3/F4). (c) The fixture's `seed` column is the data seed while the reducer seed and engine live only in the prose header (F11). A third risk (b) died with the `R (>= 3.5)` floor — corrected M48. Text in git. Promote on the next regeneration of that fixture or change to that script — the guards bite only under a future edit. Lineage: M104 review.
- **Deferred remainder of the M104 bootstrap point/interval work** — three pieces fenced out at the M104 plan gate (2026-08-05), each promoted only on evidence M104's own measurement does not answer. (a) Reconciling the reported point to its interval (clamping `conf.low` down to the estimate) — promote on a measured excluding cell whose gap falls outside M104 AC2's `1e-8` bound, or whose point estimate is materially nonzero; the 42-cell sweep found neither. (b) A containment condition in `boundary_interval_usable()` — rejected as silencing M103's hint on exactly the data M103 serves; promote if that rule is revisited. (c) Extending the sweep to the other five reducers — promote on the same shape reported against another `ci_method`. Lineage: M93 → M103 AC6 → M104.
- Two degenerate-data behaviors **deliberately kept** at the M105 plan gate (2026-08-05), each recorded in place at `R/ci-npbootstrap.R:126` as correct rather than defective. (a) `ci_method = "searle"` at `unit = "average"` on zero-between-subject-variance data reports `conf.low = conf.high = -Inf` — the correct limit of the Spearman-Brown projection, in-support under D-010's `(-Inf, 1)`. (b) `npb_guard_sb_pole()` refuses endpoints strictly past the pole (`denom < 0`) but tolerates the pole itself (`denom == 0`), pinned at `test-boundary-abort-hint.R:2240`. Reopening either supersedes D-010 and reaches all three `ci_method` values sharing that guard, so promote on a user reporting one as a defect, not on the shape alone. Lineage: M100 sweep → M105 plan gate.
- MPL κ_m **monotone envelope / smoother** over the shipped table's S dips — the calibrated κ_m is non-monotone in S at every level (worst downward step −0.068 at the shipped 0.95 [claim:kappa-worst-steps]; MC noise, bootstrap SE ≈0.03–0.04), so linear-in-S interpolation is not conservatively biased as `R/ci-mpl.R` once claimed; **chosen against at the M91 plan gate (2026-07-24)** for traceability. Every probe since cleared its frozen floor — six off-node cells across M91 and M92, one across 0.95's worst dip — so the pre-registered bracket-max consequence never fired. Promote on a cell falling below its floor, never on the non-monotonicity itself; per-cell figures in § M92 of `cairn/references/mpl-twoway-random-comparison.md`. Lineage: M88 → M90 → M91 plan gate → M91 T2 → M92 T7.
- Restore **high-side (`pmin(1, .)`) clamp detection** in the AC4 endpoint-parity test (`test-boundary-abort-hint.R`) — low-side detection survived the 2026-08-01 pole hotfix (measured at the M98 plan gate; supplier cells per class in `milestones/archive/M98-clamp-detection-assertion.md` and this row's full pre-M125 text, git), but no shipped `ci_method` can report an endpoint above +1 since `npb_guard_sb_pole()`, and the synthetic-reducer probes were rejected at that gate because they bypass the reporting path the test exists to pin. Promote on a shipped path that can again report above +1, or on a probe design that keeps the reporting path in the loop. Lineage: M93 AC4 → hotfix `hotfix-searle-pole-out-of-support` (PR #105) → M98.
- Ledger + CI checker pinning **abort-remedy truthfulness** — chosen at the M100 plan gate over a follow-on milestone; kept separate from M102 at its plan gate (the two share a row schema and checker idiom — build on M102's rather than design a second); **barred by D-021 (2026-08-03), not dropped**. Promote on a remedy bullet measurably misleading a user — a defect in what the package computes — never on the drift alone. Narrative: `milestones/archive/M100-abort-remedy-truthfulness.md`, `milestones/archive/M102-record-claims-ledger.md`. Lineage: M93 T1 → M100 → M102 plan gate → D-021.
- Harden `data-raw/check-mpl-doc-claims.py`'s recall net and self-test — M94's sub-80 findings (a trigger vocabulary missing the comparative claim class, a hard-coded absent-row probe, brittle scope anchors, structural settle assertions), plus two measured instances since: M106 F9's un-enumerated "any other design" fence claim, and the PR #115 scope gap, `news_scope` sweeping only the M94 bullet so the M88-era MPL bullet's falsified "finite interval on every dataset" sentence shipped stale — extend the NEWS scope when hardening. None affects what shipped. Text in git. Promote on the next MPL doc-surface change, or fold into the M95 pin work. Lineage: M94 review O-10/O-14/O-15/O-16/O-19–O-22 → M106 plan gate → PR #115.
- **Per-class reachability proof for the doc-claim pin** — split off M123 at its sizing gate and refused a milestone at the plan gate (2026-08-16): its deliverable is a guard over the repo's own records, which D-021 bars and D-029 declines to exempt. The eight deferred pieces (a)–(h) are named in `milestones/archive/M123-falsified-capability-claims.md`; measurements and the two mutation-harness gaps are in this row's earlier text (git). Promote on a withdrawn claim returning to a shipped surface while the pin stays green, never on a count of coverage gaps, and never without superseding D-021 first. Distinct from the `check-mpl-doc-claims.py` row above. Lineage: M123 review return 2 → sizing gate → plan gate 2026-08-16 → descope gate 2026-08-17.
- On-the-fly κ_m calibration for `ci_method = "mpl"` — the alternative to M88's precomputed table (D-015): calibrate κ_m at call time for the exact (R,S) geometry, exact but ~minutes and stochastic (needs a `seed`). Promote only on a concrete need for off-grid geometries the M88 table+interpolation doesn't serve. Lineage: D-014 (ii) → D-015 → M88.
- Companion software/methods paper (JOSS or similar) — after the v0.1.0 release; the M42 comparison article is the seed; venue/framing decided against the released package (design interview + plan gate, 2026-07-12; ADR-022 deferral) — cairn/DESIGN.md § Commitments
- Statistical-extension parking lot (grouped; see `cairn/legacy/ROADMAP.md` for the full descriptions + readiness/status per item): the `d_study()` cluster-level / occasion-ragged projection; the occasion-averaged coefficient on ragged replicates (research); incomplete/unbalanced **fixed** cluster-level `ICC(c,k)` (still blocked by ten Hove's small-`k` estimator). Promote individually via `/milestone-plan` — migrated 2026-07-12 — cairn/legacy/ROADMAP.md
- Incomplete/unbalanced fixed-rater **subject**-level multilevel lavaan — the SEM sibling that compounds two-level FIML with the Case-3A fixed correction; low priority, promote only on a concrete need. Split off from the lavaan-multilevel-siblings candidate at the M56–M58 plan gate (2026-07-17); the fixed **cluster** level stays double-blocked (parking-lot candidate above). Lineage: ADR-027 → M53 GO (D-005) → M54 → M56–M58
- Plotting: exported user-facing `theme_intraclass()` — a composable, exported ggplot2 theme users can add to their own plots. Deferred at the M61 plan gate (2026-07-17) in favor of internal-only styling; promote on a concrete request. Lineage: M61.
- Plotting: new view types beyond the three current views (e.g. a stacked variance-share / proportion chart). Deferred at the M61 plan gate (2026-07-17); multilevel views are already level-faceted, so a genuinely new display needs a concrete proposal. Lineage: M61.
- Multilevel lavaan bootstrap CI beyond balanced/complete random — two parked cells: (a) the crossed **fixed** cell (thread the per-refit Case-3A θ²_r correction through the M56 factory `lavaan_ml_simulate_refit`/`lavaan_multilevel_components`, currently random-only); (b) the **random incomplete/unbalanced** cells (the factory takes an unequal `cluster_sizes` vector, but coverage is validated only on balanced data — needs an unbalanced coverage oracle; incomplete can't bootstrap at all, ADR-031). Both MC-only ship; bootstrap parity is nice-to-have and inherits M56's cluster-level cross-platform flake. Lineage: M56 factory → M57 (fixed) → M58/MD-1 (random incomplete/unbalanced).
- lavaan + within-cell replicates — the SEM engine on replicated (σ²_sr/σ²_e-split) data. Niche, low value: would need both a lavaan replicate parameterization and the M20 replicate machinery to intersect. Promote only if a concrete need appears. Reclassified from M21 (ADR-027); promoted from the parking lot 2026-07-13 — cairn/legacy/ROADMAP.md
- d_study() CI-width precision planning ("how many subjects for a ±.1-wide interval?") — scope boundary resolved by the design interview (2026-07-12): a legitimate future direction, **gated on finding an oracle strategy**; subject-count-for-power as such stays out of scope (`M4.5-d-study.md` §6; DESIGN.md contract boundary) — cairn/estimand-specs/M4.5-d-study.md
