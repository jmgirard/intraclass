# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-08-29 (M148 merged and archived; M143 rotated out of the terminal rows and `record-claims.tsv`'s `roadmap-terminal-rows` expectation rotated with it, in this commit. ROADMAP 55 lines / 23,468 bytes, LESSONS 49 / 19,998, both under. `prose-style.md` 119 lines / 6,952 bytes against its own < 120 / < 8,000; the other three doctrine modules still state no budget (existing candidate row). Nothing retired or graduated: M148 shipped no guard and moved no content. No LESSONS line added — its one finding is an instance of the derived-claims rule tracking-rules already binds, and the archive summary names the class.)_

_Amended 2026-08-29 during the v0.1.0 submission walk: that last judgment is superseded. The class recurred in `cran-comments.md` at the win-builder step — a predicted misspelled-words list the first real run falsified — so a LESSONS line was added after all, and the file was compressed to fit its cap: LESSONS now 46 lines / 19,998 bytes (M126's dependency-claim line absorbed into M142's, the header and eight lines tightened; no lesson dropped). Also this walk: hotfix PR #160 corrected the scaffold-default maintainer address in `DESCRIPTION`, and `cran-comments.md` was rewritten against the post-hotfix runs. ROADMAP figures unchanged; no milestone rows moved._

Pre-migration history (M1–M47, ADR-001..058): see `cairn/legacy/` and git log.

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M148 | v0.1.0 is submission-ready, and the upload is handed off | done | M147 | high | milestones/archive/M148-release-v010-submission-ready.md |
| M147 | `choose_icc()` answers the type question with "both" | done | — | high | milestones/archive/M147-choose-icc-type-both.md |
| M146 | The occasion vocabulary says which quantity each surface reports | done | — | high | milestones/archive/M146-occasions-vocabulary.md |
| M145 | The v0.1.0 release-round documentation remainder | done | — | high | milestones/archive/M145-release-round-doc-remainder.md |
| M144 | NEWS's *What ships* names the designs the package actually supports | done | — | normal | milestones/archive/M144-news-what-ships-designs.md |
<!-- terminal-row retention: M148 done (2026-08-29) → M143 rotated out (oldest terminal by merge order; its archive file still resolves there). Kept: M148, M147, M146, M145, M144 (5 most recent terminal) [claim:roadmap-terminal-rows]. -->
<!-- rows grouped by status; keep only the 5 most recent terminal (done/dropped)
     rows — older history in cairn/legacy/ + git. -->


## Candidates

- **Two multilevel replicate display surfaces do not show which occasion setting a row or curve is** — (a) `format.icc()` prints two multilevel replicate rows differing only in occasion setting as indistinguishable (same `level`, same `index`, no occasion column), though D-044 records `occasions` as the SOLE disambiguator between same-`term` rows; measured 2026-08-28 on an 8x4x3x3 crossed fit. (b) On a multilevel rater projection the legend labels the cluster facet's only curve `occasions: 1` (`R/autoplot.R:64`), committing to an averaging reading the old `n_o = 1` left ambiguous; the placeholder is a no-op, not a setting. Both are `print()`/plot surface M146 did not touch and no criterion governed; neither affects what the package computes. Promote on a user reading two identical printed rows as a duplicate, or the cluster label as an averaging claim, never on the asymmetry alone. Lineage: M146 review round 1 [O] 12, round 2 [O] 6 (2026-08-28).
- **Two test-suite defects, neither a defect in what the package computes** — (a) `render_condition()` (`test-n-o-disposition-grid.R`) feeds an already-rendered `conditionMessage()` back through `cli::format_message()`, so glue re-interpolates: text holding `{1|subject:rater}` makes it throw instead of failing its case, turning the file into an error (measured 2026-08-27). No in-scope message renders braces today. Promote on a dispatch or advisory message gaining a literal brace, never on the shape alone. (b) `devtools::test()` reports `WARN 3` on the default branch (re-measured 2026-08-27 at the M143 review, which names the three sites and messages): a lavaan and a glmmTMB fitting diagnostic slipping past a narrower expectation, plus the fixed-rater advisory from an unwrapped `icc()` call; the first two are worth asserting, not silencing. Promote on a fourth joining them, or on either diagnostic marking a fit problem nothing checks. Both re-confirmed out of scope at the M143-M144 plan gate (2026-08-27): invisible to `R CMD check`, so neither blocks submission. Lineage: M141 review [O] 1; M141 hygiene.
- **M48's AC7 cross-checked workflow files while promising checks** — a dropped matrix cell or a vanished Codecov check passes it as written. A wording defect in a done milestone's criterion; nothing shipped is wrong. A standing guard over this repo's own CI config is records apparatus, barred by D-021 and not exempted by D-029; refused at the M138-M140 plan gate (2026-08-26), which instead has M139 AC3 read CI evidence from `gh api .../check-runs` against a pinned head SHA. Promote on a user reaching a platform claim a vanished matrix cell made false, never on the wording alone, and never without superseding D-021 first. The row's other eight items shipped as M138 and M140. Lineage: M48 third- and fourth-pass reviews; amendment audit.
- **Design 3's internal `design$type` and `design$model`, and its `n_raters` count** — the remainder of the absorbed Design 3 row. `design$type` is `"agreement"` and `design$model` `"twoway"` on a fit with no rater facet and one-way-style `ICC(1)`/`ICC(k)` labels, but both are internal under D-035 clause 2 and so change without a deprecation cycle, which is why M143 leaves them; `n_raters` reports 480 on the 20x6x4 fixture, a true count of nested rater ids that reads as a crossed rater count. Absorbed at the M143 hygiene pass (2026-08-27): `icc_design_phrase()` (`R/estimand.R:229`) maps a `NA` rater treatment to the literal `"one-way random"`, so a legacy `icc_dstudy` object with no `icc_design_label` attribute renders a Design 3 projection as one-way (fallbacks at `R/d-study.R:620`, `R/autoplot.R:38`). Unreachable for objects this version builds, and wrong on the default branch too, where the same object read "two-way random". Promote on a user reading `x$design` as contract, the count as a facet size, or a legacy object rendering a Design 3 header, never on the asymmetry alone. Lineage: M138 amendment audit round 1, finding 7 (2026-08-26) → promoted as M143 (2026-08-27), this remainder held back; M143 review, all three lenses (2026-08-27).
- **`glance()$n_o` is silently `NA` on an incomplete design whose cells hold equal counts** — the remainder of the absorbed `occasions`/`n_o` row. D-041 and M141's disposition grid settle the condition under which `n_o` reports a number (uniform *and* complete), and the computation is unchanged, but a user on such a design still reaches an `NA` with no explanation at the call site. The naming half of this row was reviewed twice (RB04/RR04, RB05/RR05), settled by D-044 as a documentary gap rather than a naming defect, and promoted as M146; the `NA` half was not in that scope. Promote on a user reaching an `NA` count on such a design, never on the naming alone. Lineage: M48 review second pass [O] G5; third pass [O] O1 (2026-08-25); D-044 + M146 (2026-08-28).
- **Three prose-apparatus deferrals** — barred by D-021 (a guard over this repo's own doc claims, no defect in what the package computes); earlier text in git. (a) A mechanical predicate over scope-carrying constructions — M136's descoped AC3, audit taken by M137, checker refused. Promote on a mechanical predicate, or on a user reaching a widened claim, never on a count; that clause supersedes D-021 here. (b) Hardening `data-raw/prose-profile.py`, its six blind spots listed in `cairn/doctrine/prose-style.md`; deferred at M134's gates (2026-08-23), a ruler change restarting the frozen baseline. Promote on a pass hitting one of the shapes, never on the count. (c) A two-sided sentence diff catching an invented or widened claim in a rewritten doc surface — descoped from M142 (2026-08-27) after three audit passes, the last finding it blind to swapped identifiers. Promote on a read-through missing a widened claim that reaches a user, never on the shape alone. Lineage: M136 returns 1–3 → M137 plan gate 2026-08-24; M134 fourth-pass [O] 1, 2, 7; M142 plan gate.
- **Three doctrine modules state no budget in their own header** — `data-raw-checkers.md` (68 lines / 4,180 bytes), `doc-claim-pins.md` (89 / 5,138), `source-ingestion.md` (39 / 2,185) each graduated from LESSONS without the budget the maturation exit requires, so a hygiene pass has nothing to check them against; only `prose-style.md` carries one (< 120 / < 8,000). Measured 2026-08-25. Promote on a module needing a compression decision, or on the next graduation — never on the omission alone, all three sitting under their sibling's figures. Lineage: M137 hygiene.
- **`cli` abort and hint strings get the M134 prose pass** — 135 `cli_abort`/`cli_warn`/`cli_inform` sites across 14 files, 45 of them in `R/icc.R` (corrected M137). Same D-029 class as the vignettes and roxygen, fenced out at the M134–M136 plan gate (2026-08-23): the surface is guarded by `data-raw/check-abort-remedy-verdicts.R` and pinned by rendered-message tests, so a wording pass re-keys guards a documentation pass never touches. Earlier text in git. Promote on a user reporting an abort message as hard to read, or on the next milestone that edits abort text for another reason — never on the dash count alone. Lineage: M134–M136 plan gate, criteria-audit note 2; M137 plan gate 2026-08-24.
- **The `school` fixture is rebuilt verbatim five times in `test-vignette-claims.R`** — the seeded 16x5x4 simulation at `vignettes/multilevel-designs.Rmd:45-66` is rebuilt inline at `:409`, `:477`, `:570`, `:663` and, since M132, `:731`, nothing tying a copy to the vignette chunk: a change to the article's simulation needs five test edits and reds nothing if it gets one. The file's other fixtures follow the same convention, so this is its idiom, not a defect M132 introduced. Promote on a vignette simulation change leaving a copy stale, never on the count. Lineage: M132 review [O] 11.
- **A per-`ci_method` trustworthiness table for `interval-methods.Rmd`, and a standing test that it matches the shipped surface** — M133's reverted deliverable: per `ci_method`, the family computed, refused, and verified. It failed because its cells were COMPOSED from a reading of the fences, two rounds each finding new cells false. The guard that would make it safe stays refused: D-029 retains D-021's bar (`DECISIONS.md:1300`). Promote the table on a way to DERIVE cells from code, the guard on a user reaching a misdescribed cell — never without superseding D-021 first. Prose: git, PR #142. Lineage: M133 returns 1–2 → descope 2026-08-23.
- **A `ci_method = "bootstrap"` caller is made to install `merDeriv`, and does use it** — every dispatched `fit_lme4*()` checks for `merDeriv` unconditionally before any interval-method branching, then forms the SD-scale parameter covariance eagerly (7 `vcov.lmerMod()` sites in `R/engine-lme4.R`; a traced bootstrap run calls it once, measured 2026-08-18). Sparing a bootstrap caller the dependency therefore means making the covariance conditional on the interval method — a change to the engine contract each `fit_lme4*()` returns; M16 kept the check unconditional deliberately (`033501b2`, `9ebf5ad4`). Promote on a user reporting the install as unnecessary for a bootstrap run, never on the redundancy alone. Lineage: M127 review F10 → [O] F1.
- **No standing guard that a new lme4 entry point checks for `merDeriv`** — M127's audit (12 dispatched `fit_lme4*` functions each carrying the check second and unconditionally; no `lmer`/`bootMer` call outside `R/engine-lme4.R`) is a SNAPSHOT nothing tests. A 13th entry point skipping it would leave the shipped message and `R/engine-lme4.R:50-58` silently false. Deferred at the M127 implement gate: a structural guard over this repo's own source is apparatus under D-021 pressure. Promote on a new lme4 entry point, or on a user reaching an lme4 fit that does not check — never on the count. Lineage: M127 AC4 → [O] 12 → implement gate 2026-08-18.
- **The checkpoint guard's known blind spots** — two families, full text in git; narrative in the M120 and M122 archives. Four routing blind spots (a)-(d), descoped at M120's fourth review, are declared in `data-raw/checkpoint-sites.tsv` and promised by no criterion; the sharpest: `ckpt_spec_mismatch()` compares numerics at 1.5e-8, too loose for the 1e-9 edit a digest catches. Separately the watcher traces `readRDS` only — `load()` and the other deserialization forms returned silently at the M120 re-cut audit — which M120's AC2 fences statically rather than closes. Promote on a declared site needing one of these shapes, a reversion reaching a fixture through one, or a sixth harness resuming through an untraced form — never on a count. Lineage: M120 return 4 → descope (2026-08-15) → M122 plan-gate audit (migrated M125).
- **Extend the M113 skew battery to `ci_method = "bootstrap"`** (corrected M121) — of the four methods once called unrun, only `"bootstrap"` remains: `"mpl"`/`"posterior"` abort on the one-way design by their own fences, the two-way case has its own row below, `"npbootstrap"` ran as M121. It costs 10.6–15.0 s per call — ~114 h for the 64-cell × 2000-rep grid at 4 workers — which is why M121 fenced it out; M115's caveat stays bounded to `mc`/`searle`/`burch` until it lands, and D-027's S1 clause applies. Promote on a reduced-grid design keeping the frozen floor comparable, or on a per-call cost bringing the grid under a working day. Lineage: D-027 → M115 → M121 plan gate.
- **A two-way heavy-tail grid, so the agreement-only methods can be measured at all** — `"mpl"` is fenced to the two-way random ICC(A,1)/ICC(A,k) with one rating per subject and `"posterior"` needs brms, so both refuse every cell of the one-way M113/M121 grid by design rather than by cost: nothing measured there speaks to their behaviour under skew or heavy tails. Closing it needs a second grid — a two-way random DGP per burch2011 §3, its own anchor and frozen rules, and for `"posterior"` a Stan fit per replicate, costlier than the `"bootstrap"` leg above. Promote on a need to advise users about the two-way methods under heavy tails, or on an anchor making the grid cheap. Lineage: M121 plan gate (2026-08-15).
- **Cluster-effect-direct trigger statistic family** — the reopening path D-028 names: the frozen kurtosis/skewness family failed by dilution (the pooled z carries the cluster effect at weight² ≈ 0.04 at n = 5), so a second frozen assessment would measure the cluster effect directly. Promote on a pilot showing such a statistic separates the failing cells at the D-028 floors — the evidence class that falsifies the `document` choice — never on re-thresholding the M114 family. Lineage: M114 (D-028).
- Burch's near-zero-MSA interval is **computed from floating-point roundoff** — `burch_kappa_hat()` standardizes by `sqrt(MSA)`, so at an MSA tiny but not exactly 0 both numerator and denominator are accumulated summation error. Measured at the M105 review (2026-08-05) on the committed 8×4 fixture cell: MSA = 3.52e-33, interval [−0.1865, 0.2974] at `unit = "single"`. **Pre-existing, not introduced by M105**, which is why M105's guard (`identical(MSA, 0)`) leaves it alone and M105 AC3 certifies it as a valid finite result. Promote on a user reporting such an interval, or when the M105 re-cut decides that certification should not stand. Lineage: M105 review F1.
- **M104's deferred remainder** — five items, none affecting what shipped; full text in git. Fenced out at the M104 plan gate (2026-08-05): (a) clamping the reported `conf.low` down to the point estimate — promote on an excluding cell outside M104 AC2's `1e-8` bound, none found in the 42-cell sweep; (b) a containment condition in `boundary_interval_usable()`, rejected as silencing M103's hint — promote if that rule is revisited; (c) the sweep extended to the other five reducers. Three more sat below the action bar at the M104 review (F3/F4, F11), text in git and the M104 archive; promote those on the next regeneration of that fixture or a change to its script. Lineage: M93 → M103 AC6 → M104.
- Two degenerate-data behaviors **deliberately kept** at the M105 plan gate (2026-08-05), each recorded in place at `R/ci-npbootstrap.R:126` as correct rather than defective. (a) `"searle"` at `unit = "average"` on zero-between-subject-variance data reports `conf.low = conf.high = -Inf`, the correct limit of the Spearman-Brown projection and in-support under D-010. (b) `npb_guard_sb_pole()` refuses endpoints strictly past the pole (`denom < 0`) but tolerates the pole itself (`denom == 0`), pinned at `test-boundary-abort-hint.R:2240`. Reopening either supersedes D-010 and reaches all three `ci_method` values sharing that guard, so promote on a user reporting one as a defect, not on the shape alone. Lineage: M100 sweep → M105 plan gate.
- MPL κ_m **monotone envelope / smoother** over the shipped table's S dips — the calibrated κ_m is non-monotone in S at every level (worst downward step −0.068 at the shipped 0.95 [claim:kappa-worst-steps]; MC noise, bootstrap SE ≈0.03–0.04), so linear-in-S interpolation is not conservatively biased as `R/ci-mpl.R` once claimed; **chosen against at the M91 plan gate (2026-07-24)** for traceability. Every probe since cleared its frozen floor, so the pre-registered bracket-max consequence never fired. Promote on a cell falling below its floor, never on the non-monotonicity itself; per-cell figures in § M92 of `cairn/references/mpl-twoway-random-comparison.md`. Lineage: M88 → M90 → M91 plan gate → M92 T7.
- Restore **high-side (`pmin(1, .)`) clamp detection** in the AC4 endpoint-parity test (`test-boundary-abort-hint.R`) — low-side detection survived the 2026-08-01 pole hotfix (supplier cells in `milestones/archive/M98-clamp-detection-assertion.md`), but no shipped `ci_method` can report an endpoint above +1 since `npb_guard_sb_pole()`, and the synthetic-reducer probes were rejected at that gate because they bypass the reporting path the test exists to pin. Promote on a shipped path that can again report above +1, or on a probe design that keeps the reporting path in the loop. Lineage: M93 AC4 → hotfix `hotfix-searle-pole-out-of-support` (PR #105) → M98.
- Ledger + CI checker pinning **abort-remedy truthfulness** — chosen at the M100 plan gate over a follow-on milestone; kept separate from M102 at its plan gate (the two share a row schema and checker idiom — build on M102's rather than design a second); **barred by D-021 (2026-08-03), not dropped**. Promote on a remedy bullet measurably misleading a user — a defect in what the package computes — never on the drift alone. Narrative: `milestones/archive/M100-abort-remedy-truthfulness.md`, `milestones/archive/M102-record-claims-ledger.md`. Lineage: M93 T1 → M100 → M102 plan gate → D-021.
- Harden `data-raw/check-mpl-doc-claims.py`'s recall net and self-test — M94's sub-80 findings (a trigger vocabulary missing the comparative claim class, a hard-coded absent-row probe, brittle scope anchors, structural settle assertions), plus two measured instances since: M106 F9's un-enumerated "any other design" fence claim, and the PR #115 scope gap where `news_scope` swept only the M94 bullet — extend the NEWS scope when hardening. None affects what shipped. Text in git. Promote on the next MPL doc-surface change, or fold into the M95 pin work. Lineage: M94 review O-10/O-14/O-15/O-16/O-19–O-22 → M106 plan gate → PR #115.
- **Per-class reachability proof for the doc-claim pin** — refused a milestone at the M123 plan gate (2026-08-16): its deliverable is a guard over the repo's own records, which D-021 bars and D-029 declines to exempt. The eight deferred pieces (a)–(h) are named in `milestones/archive/M123-falsified-capability-claims.md`; measurements in git. Promote on a withdrawn claim returning to a shipped surface while the pin stays green, never on a count of coverage gaps, and never without superseding D-021 first. Distinct from the `check-mpl-doc-claims.py` row above. Lineage: M123 review return 2 → plan gate 2026-08-16 → descope gate 2026-08-17.
- On-the-fly κ_m calibration for `ci_method = "mpl"` — the alternative to M88's precomputed table (D-015): calibrate κ_m at call time for the exact (R,S) geometry, exact but ~minutes and stochastic (needs a `seed`). Promote only on a concrete need for off-grid geometries the M88 table+interpolation doesn't serve. Lineage: D-014 (ii) → D-015 → M88.
- Companion software/methods paper (JOSS or similar) — after the v0.1.0 release; the M42 comparison article is the seed; venue/framing decided against the released package (design interview + plan gate, 2026-07-12; ADR-022 deferral) — cairn/DESIGN.md § Commitments
- Statistical-extension parking lot (grouped; see `cairn/legacy/ROADMAP.md` for the full descriptions + readiness/status per item): the `d_study()` cluster-level / occasion-ragged projection; the occasion-averaged coefficient on ragged replicates (research); incomplete/unbalanced **fixed** cluster-level `ICC(c,k)` (still blocked by ten Hove's small-`k` estimator). Promote individually via `/milestone-plan` — migrated 2026-07-12 — cairn/legacy/ROADMAP.md
- Incomplete/unbalanced fixed-rater **subject**-level multilevel lavaan — the SEM sibling that compounds two-level FIML with the Case-3A fixed correction; low priority, promote only on a concrete need. Split off from the lavaan-multilevel-siblings candidate at the M56–M58 plan gate (2026-07-17); the fixed **cluster** level stays double-blocked (parking-lot candidate above). Lineage: ADR-027 → M53 GO (D-005) → M54 → M56–M58
- Plotting, two deferrals from the M61 plan gate (2026-07-17): an exported user-facing `theme_intraclass()` users can add to their own plots, deferred in favor of internal-only styling — promote on a concrete request; and view types beyond the three current ones (e.g. a stacked variance-share chart) — multilevel views are already level-faceted, so a genuinely new display needs a concrete proposal. Lineage: M61.
- Multilevel lavaan bootstrap CI beyond balanced/complete random — two parked cells: (a) the crossed **fixed** cell (thread the per-refit Case-3A θ²_r correction through the M56 factory `lavaan_ml_simulate_refit`/`lavaan_multilevel_components`, currently random-only); (b) the **random incomplete/unbalanced** cells (the factory takes an unequal `cluster_sizes` vector, but coverage is validated only on balanced data — needs an unbalanced coverage oracle; incomplete can't bootstrap at all, ADR-031). Both MC-only ship; bootstrap parity inherits M56's cluster-level cross-platform flake. Lineage: M56 factory → M57 (fixed) → M58/MD-1 (random incomplete/unbalanced).
- lavaan + within-cell replicates — the SEM engine on replicated (σ²_sr/σ²_e-split) data. Niche, low value: would need both a lavaan replicate parameterization and the M20 replicate machinery to intersect. Promote only if a concrete need appears. Reclassified from M21 (ADR-027); promoted from the parking lot 2026-07-13 — cairn/legacy/ROADMAP.md
- d_study() CI-width precision planning ("how many subjects for a ±.1-wide interval?") — scope boundary resolved by the design interview (2026-07-12): a legitimate future direction, **gated on finding an oracle strategy**; subject-count-for-power as such stays out of scope (`M4.5-d-study.md` §6; DESIGN.md contract boundary) — cairn/estimand-specs/M4.5-d-study.md
