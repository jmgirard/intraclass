# M95 mutation record — whole-table kappa_m pin (AC4/AC5)

Generated by `data-raw/m95-mutation-check.R` on 2026-07-31.

Target: the M95 AC2 pin in `tests/testthat/test-ci-mpl.R`
(`"the shipped kappa_m table matches its whole-table fixture, every cell (M95 AC2)"`).
The pin is a symmetric `identical()`/key-set comparison between the shipped
`kappa_m_table` and `tests/testthat/fixtures/kappa-m-table.txt`, so a
discrepancy introduced on either side is the same detected event; cases 1–5
mutate the fixture side, case 6 rewrites `R/sysdata.rda` itself (the
scenario the pin exists for) and reloads the package. Baseline before and
restored state after both ran green, and in every mutated run the pin was
the ONLY test in the file that failed (asserted by the script).

| # | Side | Cell | Perturbation | Pin result |
|---|---|---|---|---|
| 1 | fixture | (2, 10, 0.90) | kappa_m + 0.5 | RED — 1 failed expectation |
| 2 | fixture | (10, 100, 0.99) | kappa_m + 0.5 | RED — 1 failed expectation |
| 3 | fixture | (6, 15, 0.95) -- covered by NO pre-M95 literal pin | kappa_m + 0.5 | RED — 1 failed expectation |
| 4 | fixture | (2, 12, 0.95) -- not a shipped node | row added, kappa_m = 1.0 | RED — 5 failed expectations |
| 5 | fixture | (3, 20, 0.90) | row removed | RED — 5 failed expectations |
| 6 | shipped (R/sysdata.rda) | (6, 15, 0.95) in the shipped table itself | kappa_m + 0.5, fixture intact | RED — 1 failed expectation |

Per case, the first failed expectation as reported by testthat:

**Case 1 — perturb (2,10,0.90).**

```
Expected `shipped$kappa_m` to be identical to `fixture$kappa_m`.
Differences:
  `actual[1:4]`: 0.93 1.15 1.35 1.46
`expected[1:4]`: 1.43 1.15 1.35 1.46
```

**Case 2 — perturb (10,100,0.99).**

```
Expected `shipped$kappa_m` to be identical to `fixture$kappa_m`.
Differences:
  `actual[159:162]`: 0.11 0.18 0.15 0.20
`expected[159:162]`: 0.11 0.18 0.15 0.70
```

**Case 3 — perturb (6,15,0.95) [AC5].**

```
Expected `shipped$kappa_m` to be identical to `fixture$kappa_m`.
Differences:
  `actual[77:83]`: 0.44 0.44 0.20 0.23 0.29 0.31 0.33
`expected[77:83]`: 0.44 0.44 0.20 0.73 0.29 0.31 0.33
```

**Case 4 — add row (2,12,0.95).**

```
Expected `key(shipped)` to be identical to `key(fixture)`.
Differences:
     actual      | expected        
[22] "2|10|0.90" | "2|10|0.90" [22]
[23] "2|10|0.95" | "2|10|0.95" [23]
[24] "2|10|0.99" | "2|10|0.99" [24]
                 - "2|12|0.95" [25]
[25] "2|15|0.90" | "2|15|0.90" [26]
```

**Case 5 — drop row (3,20,0.90).**

```
Expected `key(shipped)` to be identical to `key(fixture)`.
Differences:
     actual      | expected        
[43] "3|15|0.90" | "3|15|0.90" [43]
[44] "3|15|0.95" | "3|15|0.95" [44]
[45] "3|15|0.99" | "3|15|0.99" [45]
[46] "3|20|0.90" -                 
[47] "3|20|0.95" | "3|20|0.95" [46]
```

**Case 6 — perturb shipped (6,15,0.95).**

```
Expected `shipped$kappa_m` to be identical to `fixture$kappa_m`.
Differences:
  `actual[77:83]`: 0.44 0.44 0.20 0.73 0.29 0.31 0.33
`expected[77:83]`: 0.44 0.44 0.20 0.23 0.29 0.31 0.33
```

Case 3 is the AC5 case: its cell (6, 15, 0.95) is covered by no pre-M95
literal pin — it sits in the +0.5-on-unpinned-cells set that M92's pass-6
mutation (finding P6-1) showed leaves the pre-M95 `test-ci-mpl.R` at
FAIL 0 / PASS 172 — and the whole-table pin reds on it.
