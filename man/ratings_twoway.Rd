% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{ratings_twoway}
\alias{ratings_twoway}
\title{Simulated balanced two-way ratings, twenty subjects by four raters}
\format{
A data frame with 80 rows and 3 columns, as in \link{ratings}
(\code{subject}, \code{rater}, \code{score}).
}
\source{
Simulated by \code{data-raw/make-vignette-data.R} with \code{set.seed(88)}.
The score is a subject effect plus a rater effect plus noise, all normal
draws with the variances above.
}
\usage{
ratings_twoway
}
\description{
Simulated data, not a real study. Twenty subjects are each scored once by
the same four raters. The variance components are 0.6 (subject), 0.1
(rater), and 0.2 (residual), so the population \code{ICC(A,1)} is 0.667. The
design is large enough for \code{ci_method = "mpl"}, which the six-subject
\link{ratings} data are not. The "Interval methods" article uses it.
}
\examples{
str(ratings_twoway)
icc(ratings_twoway, score, subject, rater, type = "agreement",
  ci_method = "mpl")
}
\seealso{
\link{ratings} for the Shrout and Fleiss worked example.
}
\keyword{datasets}
