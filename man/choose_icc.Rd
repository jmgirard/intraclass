% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choose-icc.R
\name{choose_icc}
\alias{choose_icc}
\alias{format.icc_recommendation}
\alias{print.icc_recommendation}
\title{Recommend an ICC and the call that computes it}
\usage{
choose_icc(
  model = NULL,
  type = NULL,
  unit = NULL,
  raters = NULL,
  multilevel = NULL,
  level = NULL
)

\method{format}{icc_recommendation}(x, ...)

\method{print}{icc_recommendation}(x, ...)
}
\arguments{
\item{model}{\code{"twoway"} (crossed: the same raters judge every subject) or
\code{"oneway"} (raters are interchangeable across subjects). Defaults to
\code{"twoway"}. Under \code{"oneway"} the \code{type} and \code{raters} choices do not exist
(there is no rater term), and supplying them is an error.}

\item{type}{\code{"agreement"} (the value itself must match; systematic rater
offsets count as error) or \code{"consistency"} (only rank order matters; a
constant per-rater offset is forgiven). Required for a two-way design.}

\item{unit}{\code{"single"} (you will act on one rater's score), \code{"average"} (the
mean of your raters), or \code{"both"}. Required.}

\item{raters}{\code{"random"} (a sample you generalize beyond -- the recommended
default for interrater reliability) or \code{"fixed"} (exactly these judges, no
generalization). Required for a two-way design.}

\item{multilevel}{\code{TRUE} if subjects are nested in higher-level clusters
(pupils in classrooms, patients in clinics), else \code{FALSE} (the default).}

\item{level}{For a multilevel design, \code{"subject"} (within-cluster reliability),
\code{"cluster"} (between-cluster reliability), or \code{"both"}. Required when
\code{multilevel = TRUE}.}

\item{x}{An \code{icc_recommendation} object.}

\item{...}{Unused, for method consistency.}
}
\value{
An \code{icc_recommendation} object (a list) with a \code{\link[=print]{print()}} method. It
carries the recommended coefficient rows (\verb{$rows}), the exact \code{icc()} call as
a string (\verb{$call}), the per-decision rationale (\verb{$rationale}), and any notes
(\verb{$notes}).
}
\description{
\code{choose_icc()} walks the decision tree of the \emph{Choosing an ICC} vignette and
returns a recommendation object naming the coefficient(s) to report and the
exact \code{\link[=icc]{icc()}} call that computes them. It does \strong{not} fit a model: there is no
\code{data} argument. Copy the emitted call and run it on your data.
}
\details{
Supply the decisions as arguments to get advice programmatically; call
\code{choose_icc()} with the relevant answers omitted in an interactive session to
be asked the outstanding questions one at a time.

The two structural facts about your design -- whether the raters are crossed
(\code{model}) and whether subjects are nested in clusters (\code{multilevel}) -- default
to the common case (a crossed, non-multilevel two-way design), matching
\code{\link[=icc]{icc()}}. The choices that actually select the coefficient (\code{type}, \code{unit},
\code{raters}, and \code{level} when multilevel) have no silent default: in a
non-interactive session, leaving one unanswered is an error naming the
unanswered decision (rather than quietly picking one for you).
}
\examples{
# Two-way absolute agreement, single rater, random raters (Shrout & Fleiss
# ICC(2,1)): the two structural defaults (crossed, non-multilevel) are implied.
choose_icc(type = "agreement", unit = "single", raters = "random")

# Consistency of the average of fixed raters -- McGraw & Wong ICC(C,k):
choose_icc(type = "consistency", unit = "average", raters = "fixed")

# A one-way design (interchangeable raters): type/raters do not apply.
choose_icc(model = "oneway", unit = "single")

# A multilevel design, both levels:
choose_icc(type = "agreement", unit = "single", raters = "random",
  multilevel = TRUE, level = "both")
}
\seealso{
\code{\link[=icc]{icc()}}, and \code{vignette("choosing-an-icc")} for the full decision tree.
}
